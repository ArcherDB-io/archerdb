<!-- This file is generated by [/src/scripts/client_readmes.zig](/src/scripts/client_readmes.zig). -->
# Polygon Query Python Sample

Code for this sample is in [./main.py](./main.py).

## Prerequisites

Linux >= 5.6 is the only production environment we
support. But for ease of development we also support macOS and Windows.
* Python (or PyPy, etc) >= `3.7`

## Setup

First, clone this repo and `cd` into `archerdb/src/clients/python/samples/polygon-query`.

Then, install the ArcherDB client:

```console
pip install archerdb
```

## Start the ArcherDB server

Follow steps in the repo README to [run
ArcherDB](/README.md#running-archerdb).

If you are not running on port `localhost:3000`, set
the environment variable `ARCHERDB_ADDRESS` to the full
address of the ArcherDB server you started.

## Run this sample

Now you can run this sample:

```console
python3 main.py
```

## Walkthrough

Here's what this project does.

## 1. Insert GeoEvents

This project inserts GeoEvents in and around a polygonal geofence.

## 2. Query by polygon

It queries the polygon and validates that only events inside the
geofence are returned.
