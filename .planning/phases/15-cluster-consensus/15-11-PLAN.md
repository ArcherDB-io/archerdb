---
phase: 15-cluster-consensus
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/archerdb/cluster_metrics.zig
  - src/archerdb/metrics_server.zig
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "HTTP requests receive 429 responses when load shedding is active"
    - "Overload 429 responses include a Retry-After header derived from retry_after_ms"
  artifacts:
    - path: "src/archerdb/cluster_metrics.zig"
      provides: "Tracks latest load-shed retry-after value for HTTP responses"
      contains: "archerdb_shed_retry_after_last_ms"
    - path: "src/archerdb/metrics_server.zig"
      provides: "HTTP overload handling with Retry-After header"
      contains: "Retry-After"
  key_links:
    - from: "src/vsr/replica.zig"
      to: "src/archerdb/cluster_metrics.zig"
      via: "recordShedRequest(retry_after_ms)"
      pattern: "recordShedRequest\\(retry_after_ms\\)"
    - from: "src/archerdb/metrics_server.zig"
      to: "src/archerdb/cluster_metrics.zig"
      via: "Retry-After header uses latest retry-after gauge"
      pattern: "Retry-After"
---

<objective>
Close the remaining load-shedding gap by mapping overload decisions to HTTP 429 responses with Retry-After headers.

Purpose: Ensure CLUST-03 is fully satisfied by emitting client-visible overload signals on the HTTP request path.
Output: HTTP overload response wiring and retry-after tracking for metrics server responses.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cluster-consensus/15-VERIFICATION.md
@.planning/phases/15-cluster-consensus/15-03-SUMMARY.md

@src/archerdb/cluster_metrics.zig
@src/archerdb/metrics_server.zig
@src/vsr/replica.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track latest Retry-After value for shed requests</name>
  <files>src/archerdb/cluster_metrics.zig</files>
  <action>
    Add a new gauge (e.g., `archerdb_shed_retry_after_last_ms`) alongside the other load-shedding metrics to store the latest retry-after milliseconds for overload responses.
    Update `recordShedRequest(retry_after_ms)` to set the new gauge to the provided retry_after_ms before recording the histogram, keeping units in milliseconds for HTTP header use.
    Extend `ClusterMetrics.format` to export the new gauge, and update the load-shedding tests to re-initialize the gauge for isolation and assert the new metric appears in formatted output (and reflects the latest retry_after_ms where applicable).
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "ClusterMetrics"</verify>
  <done>Latest retry-after milliseconds are stored in a gauge, exported in cluster metrics output, and tests cover the new metric.</done>
</task>

<task type="auto">
  <name>Task 2: Emit 429 + Retry-After on HTTP overload responses</name>
  <files>src/archerdb/metrics_server.zig</files>
  <action>
    Add overload detection in `handleRequest` (before routing) that reads `archerdb_shed_score` and `archerdb_shed_threshold` (scaled 0-100) from `cluster_metrics`. Treat threshold==0 as uninitialized and skip shedding in that case; otherwise, treat `score >= threshold` as overloaded.
    When overloaded, read the latest retry-after milliseconds from the new gauge, convert to whole seconds with a minimum of 1 (ceil division), and respond with HTTP 429 plus a `Retry-After: <seconds>` header.
    Implement a `sendResponseWithHeaders` helper (or extend `sendResponse`) to include optional headers without changing existing call sites. Keep existing responses unchanged for non-overload cases.
    Add a unit test that exercises the overload response path (or the new helper) to assert the status line is `429 Too Many Requests` and the `Retry-After` header is present with the expected seconds value.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "MetricsServer"</verify>
  <done>Overload HTTP responses return 429 with a Retry-After header derived from the load-shed retry-after gauge.</done>
</task>

</tasks>

<verification>
- `./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "ClusterMetrics"`
- `./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "MetricsServer"`
</verification>

<success_criteria>
- HTTP requests receive a 429 response when load shedding indicates overload.
- Retry-After header is emitted with a seconds value derived from the latest shed retry_after_ms.
- Cluster metrics export the latest retry-after gauge for observability and server use.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cluster-consensus/15-11-SUMMARY.md`
</output>
