---
phase: 15-cluster-consensus
plan: 10
type: execute
wave: 5
depends_on:
  - 15-02
  - 15-04
  - 15-09
files_modified:
  - src/archerdb/cli.zig
  - src/archerdb/main.zig
  - src/vsr/replica.zig
  - src/vsr.zig
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "VSR timeouts are configurable via cloud/datacenter/custom profiles with jitter"
    - "Phase-1 and phase-2 quorums are independently configurable and validated"
  artifacts:
    - path: "src/vsr/replica.zig"
      provides: "TimeoutConfig and QuorumConfig integrated into replica init"
    - path: "src/archerdb/cli.zig"
      provides: "CLI flags for timeout profiles and quorum presets"
    - path: "src/vsr/timeout_profiles.zig"
      provides: "TimeoutConfig used at runtime"
    - path: "src/vsr/flexible_paxos.zig"
      provides: "QuorumConfig validation used at runtime"
  key_links:
    - from: "src/vsr/replica.zig"
      to: "src/vsr/timeout_profiles.zig"
      via: "TimeoutConfig.getEffectiveValues/applyJitter"
      pattern: "TimeoutConfig"
    - from: "src/vsr/replica.zig"
      to: "src/vsr/flexible_paxos.zig"
      via: "QuorumConfig.validate"
      pattern: "validateQuorums"
---

<objective>
Wire timeout profiles and flexible Paxos quorums into replica configuration.

Purpose: Activate the profile-based timeout tuning and independent quorum configuration to meet CLUST-02 and CLUST-05 requirements.
Output: CLI-configurable TimeoutConfig + QuorumConfig are applied during replica init with validation and jitter.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cluster-consensus/15-VERIFICATION.md
@.planning/phases/15-cluster-consensus/15-02-SUMMARY.md
@.planning/phases/15-cluster-consensus/15-04-SUMMARY.md

# Reference code
@src/vsr/timeout_profiles.zig
@src/vsr/flexible_paxos.zig
@src/vsr/replica.zig
@src/archerdb/cli.zig
@src/archerdb/main.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply timeout profiles during replica initialization</name>
  <files>src/archerdb/cli.zig, src/archerdb/main.zig, src/vsr/replica.zig</files>
  <action>
Expose timeout profile configuration and apply it to VSR timeouts with jitter.

- Add CLI flags for timeout profiles: `--vsr-timeout-profile` (cloud|datacenter|custom), `--vsr-timeout-jitter-pct`, and override flags for heartbeat/election/request/connection/view-change timeouts
- Extend `Command.Start` to carry a `TimeoutConfig` (profile + overrides) and parse args into it
- Pass `timeout_config` through `main.zig` into `Replica.OpenOptions`
- In `Replica.init`, call `TimeoutConfig.getEffectiveValues()` and apply `applyJitter()` when setting timeouts such as `normal_heartbeat_timeout`, `start_view_change_window_timeout`, `view_change_status_timeout`, `request_start_view_message_timeout`, and `prepare_timeout`
- Keep existing per-timeout overrides (`timeout_prepare_ticks`, `timeout_grid_repair_message_ticks`) as higher precedence if provided
  </action>
  <verify>Build passes: `./zig/zig build -j4 -Dconfig=lite check`</verify>
  <done>Replica initialization uses TimeoutConfig profile values with jitter and optional overrides</done>
</task>

<task type="auto">
  <name>Task 2: Integrate flexible Paxos quorum configuration</name>
  <files>src/archerdb/cli.zig, src/archerdb/main.zig, src/vsr/replica.zig, src/vsr.zig</files>
  <action>
Allow operators to set phase-1/phase-2 quorums and wire them into replica quorum calculations.

- Add CLI options for quorum preset (`--vsr-quorum-preset=classic|fast_commit|strong_leader`) and explicit overrides (`--vsr-quorum-phase1`, `--vsr-quorum-phase2`)
- Build a `flexible_paxos.QuorumConfig` from preset/overrides in CLI parsing and pass it into `Replica.OpenOptions`
- In `Replica.init`, replace `vsr.quorums(replica_count)` with config-aware values:
  - Validate with `flexible_paxos.validateQuorums()` (enforces Q1+Q2>N)
  - Set `quorum_view_change = config.phase1_quorum` and `quorum_replication = config.phase2_quorum`
  - Recompute `quorum_nack_prepare` and `quorum_majority` based on the configured quorums
- Add a helper in `vsr.zig` if needed (e.g., `quorumsFromConfig`) to keep quorum math centralized
  </action>
  <verify>Build passes: `./zig/zig build -j4 -Dconfig=lite check`</verify>
  <done>Replica uses configurable phase-1/phase-2 quorums and validates Q1+Q2>N at config load</done>
</task>

</tasks>

<verification>
1. Build succeeds: `./zig/zig build -j4 -Dconfig=lite check`
2. Timeout profile tests still pass: `./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "timeout_profiles"`
3. Flexible paxos tests still pass: `./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FlexiblePaxos"`
</verification>

<success_criteria>
- TimeoutConfig profiles/overrides influence replica timeouts with jitter
- Flexible Paxos quorums are configurable and validated at startup
- CLI exposes profile/preset configuration options for operators
</success_criteria>

<output>
After completion, create `.planning/phases/15-cluster-consensus/15-10-SUMMARY.md`
</output>
