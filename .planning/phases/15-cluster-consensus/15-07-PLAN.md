---
phase: 15-cluster-consensus
plan: 07
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/archerdb/metrics.zig
  - src/message_bus.zig
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Server accepts client connections through pool infrastructure"
    - "Cluster metrics are exported for Prometheus scraping"
  artifacts:
    - path: "src/message_bus.zig"
      provides: "Connection accept path uses ServerConnectionPool acquire/release"
    - path: "src/archerdb/metrics.zig"
      provides: "Registry exports ClusterMetrics.format output"
  key_links:
    - from: "src/message_bus.zig"
      to: "src/connection_pool.zig"
      via: "ServerConnectionPool.acquire/release in accept/terminate paths"
      pattern: "ServerConnectionPool\(Connection\)"
    - from: "src/archerdb/metrics.zig"
      to: "src/archerdb/cluster_metrics.zig"
      via: "Registry.format calls ClusterMetrics.format"
      pattern: "ClusterMetrics\.format"
---

<objective>
Wire the connection pool into the server accept path and export cluster metrics.

Purpose: Close the integration gaps so the pool actively controls incoming connections and the dashboard receives pool metrics from the registry.
Output: MessageBus accept path uses ServerConnectionPool, and Registry.format emits ClusterMetrics output.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cluster-consensus/15-VERIFICATION.md
@.planning/phases/15-cluster-consensus/15-01-SUMMARY.md

# Reference code
@src/connection_pool.zig
@src/message_bus.zig
@src/archerdb/metrics.zig
@src/archerdb/cluster_metrics.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export ClusterMetrics via Registry.format</name>
  <files>src/archerdb/metrics.zig</files>
  <action>
Add ClusterMetrics to the global metrics registry so Prometheus exports include pool/shed/routing series.

- Import `cluster_metrics.zig` and add a registry-owned instance (e.g., `pub var cluster_metrics = ClusterMetrics.init()`)
- Expose a stable accessor (e.g., `pub fn clusterMetrics() *ClusterMetrics { return &cluster_metrics; }`) for other subsystems
- Update `Registry.format` to call `cluster_metrics.format(writer)` after the core metric blocks and add a newline separator
- Keep format ordering consistent with other metric groups (write section break before/after ClusterMetrics)
  </action>
  <verify>Build passes: `./zig/zig build -j4 -Dconfig=lite check`</verify>
  <done>Registry.format outputs `archerdb_pool_*`, `archerdb_shed_*`, and `archerdb_routing_*` series</done>
</task>

<task type="auto">
  <name>Task 2: Route accepts through ServerConnectionPool</name>
  <files>src/message_bus.zig</files>
  <action>
Integrate ServerConnectionPool into the replica accept path so incoming client connections are acquired/released from the pool.

- Import `connection_pool.zig` and define `const ServerConnectionPool = connection_pool.ServerConnectionPool(Connection);` inside `MessageBusType`
- Add a `client_pool` field to MessageBus (replica-only) initialized in `MessageBus.init` using default `PoolConfig` and `metrics.Registry.clusterMetrics()`
- Implement pool factory/destructor to reuse existing `Connection` slots (select a `.free` slot for acquire, reset on release) without changing Connection layout
- In `accept()`/`accept_callback`, acquire from `client_pool` before reserving a connection; on pool exhaustion or timeout, close the socket and record timeout metric
- In `terminate_close_callback`, call `client_pool.release()` after resetting the connection to `.free` so idle counts track correctly
- Ensure `connections_used` stays consistent with pool active/idle counts (do not double-count)
  </action>
  <verify>Build passes: `./zig/zig build -j4 -Dconfig=lite check`</verify>
  <done>Accepted client connections flow through ServerConnectionPool acquire/release and pool metrics update during lifecycle</done>
</task>

</tasks>

<verification>
1. Build succeeds: `./zig/zig build -j4 -Dconfig=lite check`
2. Metrics output includes cluster metrics: run `./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "ClusterMetrics"`
</verification>

<success_criteria>
- MessageBus accept path uses ServerConnectionPool for client connections
- ClusterMetrics.format output is included in metrics registry export
- Pool metrics update as connections are acquired and released
</success_criteria>

<output>
After completion, create `.planning/phases/15-cluster-consensus/15-07-SUMMARY.md`
</output>
