---
phase: 09-testing-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unit_tests.zig
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "Unit tests pass 100% with no failures"
    - "Integration tests pass 100%"
    - "CI reports clear pass/fail status for each test category"
  artifacts:
    - path: "src/unit_tests.zig"
      provides: "Quine-generated test manifest"
      contains: "comptime"
    - path: ".github/workflows/ci.yml"
      provides: "CI test orchestration"
      contains: "test:unit"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "src/unit_tests.zig"
      via: "zig build test:unit"
      pattern: "test:unit"
---

<objective>
Fix any failing or flaky unit and integration tests to achieve 100% pass rate.

Purpose: TEST-01 and TEST-02 require 100% pass rate with no flaky tests. The existing test infrastructure is mature but may have intermittent failures that need investigation and resolution.
Output: All unit and integration tests pass consistently in CI and locally.
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-infrastructure/09-CONTEXT.md
@.planning/phases/09-testing-infrastructure/09-RESEARCH.md
@src/unit_tests.zig
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix unit test failures</name>
  <files>src/unit_tests.zig, various test files</files>
  <action>
Run `./zig/zig build -j4 -Dconfig=lite test:unit` and capture any failures.

For each failing test:
1. Identify if failure is deterministic or flaky (run 3 times)
2. If deterministic: fix the test or underlying code
3. If flaky: apply tick-based timing (not wall-clock), fixed seed 42 pattern

Known issues from STATE.md to check:
- Pre-existing quine test failure in unit_tests.zig
- Cluster-based tests may fail with 32KB block_size

Do NOT skip or quarantine tests. Fix them or document why they cannot run in lite config (add CI-only annotation like MULTI-04/05/06 pattern).

Ensure quine test regenerates unit_tests.zig correctly if any test files have been added/removed.
  </action>
  <verify>
`./zig/zig build -j4 -Dconfig=lite test:unit` passes with exit code 0.
Run 3 times to confirm no flakiness.
  </verify>
  <done>
All unit tests pass 100% consistently. Any tests that require CI-only config are documented with annotation pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix integration test failures</name>
  <files>src/integration_tests.zig, src/testing/*.zig</files>
  <action>
Run `./zig/zig build -j4 -Dconfig=lite test:integration` and capture any failures.

For each failing test:
1. Identify root cause (missing service dependency, config issue, timing)
2. Apply appropriate fix following established patterns:
   - Use TestContext.init()/deinit() for isolation
   - Use tick-based timing, not wall-clock
   - Use fixed seed 42 for determinism
   - Clean up temp files/directories

Integration tests may require:
- MinIO service for S3 tests (document as CI-only if needed)
- ArcherDB binary built (ensure build step runs first)

Document any tests that require external services with clear annotations.
  </action>
  <verify>
`./zig/zig build -j4 -Dconfig=lite test:integration` passes (or reports expected skips for service-dependent tests).
Run 3 times to confirm no flakiness.
  </verify>
  <done>
Integration tests pass 100%. Service-dependent tests clearly documented with CI-only annotations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update CI workflow for test reliability</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Review and update CI workflow to ensure test reliability:

1. Add retry logic for transient network failures (actions/checkout, downloads)
2. Add explicit timeouts to prevent hung tests:
   - Unit tests: 20 minutes max
   - Integration tests: 30 minutes max
3. Ensure test failures produce clear error messages (capture test output)
4. Add test step that aggregates results for easy review

Do NOT change test parallelism (matrix strategy already good).
Do NOT add new test categories (that's for later plans).

Keep changes minimal - focus on reliability, not features.
  </action>
  <verify>
`yamllint .github/workflows/ci.yml` passes (or no syntax errors).
Workflow has timeout-minutes on test jobs.
  </verify>
  <done>
CI workflow has explicit timeouts and retry logic for reliability.
  </done>
</task>

</tasks>

<verification>
1. `./zig/zig build -j4 -Dconfig=lite test:unit` - passes 3/3 runs
2. `./zig/zig build -j4 -Dconfig=lite test:integration` - passes 3/3 runs
3. CI workflow syntax valid
4. No tests marked @skip or quarantined
</verification>

<success_criteria>
- TEST-01: Unit tests pass 100% with no flaky tests
- TEST-02: Integration tests pass 100%
- All failures investigated and fixed or documented as config-specific
- CI workflow has reliability improvements (timeouts, retries)
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-infrastructure/09-01-SUMMARY.md`
</output>
