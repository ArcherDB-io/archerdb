---
phase: 09-testing-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/vopr.yml
  - .github/ci/run-vopr.sh
autonomous: true

must_haves:
  truths:
    - "VOPR runs 10+ seeds without assertion failures"
    - "VOPR seeds are deterministic and reproducible"
    - "CI runs quick VOPR on PRs and full VOPR daily"
  artifacts:
    - path: ".github/workflows/vopr.yml"
      provides: "VOPR CI workflow"
      contains: "vopr-geo"
    - path: ".github/ci/run-vopr.sh"
      provides: "VOPR runner script"
      contains: "DURATION_SECS"
  key_links:
    - from: ".github/workflows/vopr.yml"
      to: ".github/ci/run-vopr.sh"
      via: "workflow calls script"
      pattern: "run-vopr.sh"
---

<objective>
Configure VOPR fuzzing to run 10+ seeds clean for TEST-03 validation.

Purpose: VOPR (Verification-Oriented Property Replication) is the deterministic simulator that finds consensus bugs. Running multiple seeds increases coverage while maintaining reproducibility.
Output: CI workflow runs VOPR with multiple seeds, logs failures with seed for reproduction.
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-infrastructure/09-CONTEXT.md
@.planning/phases/09-testing-infrastructure/09-RESEARCH.md
@.github/workflows/vopr.yml
@.github/ci/run-vopr.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance VOPR runner for multi-seed testing</name>
  <files>.github/ci/run-vopr.sh</files>
  <action>
Update the VOPR runner script to support running multiple seeds:

1. Add --seeds argument to specify number of seeds (default: 10 for TEST-03)
2. Implement seed selection strategy:
   - Use sequential seeds: 42, 43, 44, ..., 51 (predictable, reproducible)
   - Alternatively: derive from Git SHA (current pattern) plus offset
3. Run each seed for equal time slice: duration / num_seeds
4. Aggregate results: pass only if ALL seeds pass
5. On failure: log exact seed and state for reproduction

Example enhancement:
```bash
SEEDS="${3:-10}"  # Default 10 seeds for TEST-03
DURATION_PER_SEED=$((DURATION_SECS / SEEDS))

for i in $(seq 0 $((SEEDS - 1))); do
  SEED=$((42 + i))  # Sequential seeds starting at 42
  run_single_seed "$SEED" "$DURATION_PER_SEED"
done
```

Keep backward compatibility: single seed mode still works.
  </action>
  <verify>
`.github/ci/run-vopr.sh geo 300 10` runs 10 seeds for 30 seconds each.
Script exits 0 if all seeds pass, 1 if any seed fails.
  </verify>
  <done>
VOPR runner supports multi-seed execution with configurable count.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add quick VOPR to PR workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add a quick VOPR smoke test to the main CI workflow for PRs:

1. Add new job `vopr-quick` that depends on `smoke` (fast feedback)
2. Run VOPR for 5 minutes with 5 seeds (1 minute per seed)
3. Run both geo and testing state machines in parallel
4. Make it non-blocking initially (continue-on-error: true) until validated stable

```yaml
vopr-quick:
  needs: smoke
  runs-on: ubuntu-latest
  timeout-minutes: 15
  strategy:
    fail-fast: false
    matrix:
      state_machine: [geo, testing]
  steps:
    - uses: actions/checkout@v4
    - name: Download Zig
      run: ./zig/download.sh
    - name: Run VOPR quick smoke
      run: ./.github/ci/run-vopr.sh ${{ matrix.state_machine }} 300 5
      continue-on-error: true  # Remove after validation
```

Keep existing daily VOPR workflow unchanged (2-hour deep runs).
  </action>
  <verify>
CI workflow YAML is valid (no syntax errors).
VOPR quick job appears in workflow file.
  </verify>
  <done>
PR CI includes 5-minute VOPR smoke test with 5 seeds each state machine.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update daily VOPR for 10+ seeds</name>
  <files>.github/workflows/vopr.yml</files>
  <action>
Update the scheduled daily VOPR workflow to run 10+ seeds:

1. Change default duration to allow 10 seeds with meaningful runtime:
   - 2 hours total = 7200 seconds
   - 10 seeds = 720 seconds (12 minutes) per seed
   - This is enough for meaningful coverage

2. Update both geo and testing jobs:
   - Pass seeds count: `.github/ci/run-vopr.sh $STATE_MACHINE $DURATION 10`
   - Ensure logs include seed number for each run

3. Add artifact upload for ALL seed logs (not just on failure):
   - Helps identify patterns in which seeds find issues
   - Retention: 30 days (current setting is good)

4. Add workflow_dispatch input for seeds:
```yaml
inputs:
  seeds:
    description: 'Number of seeds to run (default: 10)'
    required: false
    default: '10'
```
  </action>
  <verify>
`.github/workflows/vopr.yml` is valid YAML.
Workflow dispatch includes seeds input.
Jobs pass seeds parameter to run-vopr.sh.
  </verify>
  <done>
Daily VOPR runs 10 seeds per state machine. TEST-03 satisfied.
  </done>
</task>

</tasks>

<verification>
1. Run VOPR locally: `.github/ci/run-vopr.sh testing 120 3` (2 min, 3 seeds) - should complete
2. Verify script outputs seed number in logs
3. Verify CI workflow YAML syntax is valid
4. Verify workflow_dispatch includes seeds parameter
</verification>

<success_criteria>
- TEST-03: VOPR fuzzing runs 10+ seeds clean (no assertion failures)
- Seeds are deterministic (42, 43, ..., 51)
- PR CI has quick VOPR smoke (5 min, 5 seeds)
- Daily VOPR runs full 10 seeds for 2 hours total
- Failures log exact seed for reproduction
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-infrastructure/09-02-SUMMARY.md`
</output>
