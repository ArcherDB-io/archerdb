---
phase: 04-fault-tolerance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vsr/fault_tolerance_test.zig
autonomous: true

must_haves:
  truths:
    - "Network partitions don't cause data loss"
    - "Packet loss and latency spikes don't cause data corruption"
  artifacts:
    - path: "src/vsr/fault_tolerance_test.zig"
      provides: "FAULT-05, FAULT-06 network fault tests"
      contains: "FAULT-05"
  key_links:
    - from: "src/vsr/fault_tolerance_test.zig"
      to: "src/testing/cluster/network.zig"
      via: "drop_all/pass_all for partition simulation"
      pattern: "drop_all"
    - from: "src/vsr/fault_tolerance_test.zig"
      to: "src/testing/packet_simulator.zig"
      via: "Packet loss and latency injection"
      pattern: "path_clog"
---

<objective>
Add tests validating network partition resilience and packet loss/latency handling (FAULT-05, FAULT-06).

Purpose: Verify that network partitions don't cause data loss (majority continues, minority catches up after heal) and that packet loss/latency spikes don't cause data corruption. The existing infrastructure has drop_all/pass_all for partitions and packet simulator for network faults.

Output: FAULT-05 and FAULT-06 tests added to `src/vsr/fault_tolerance_test.zig`
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-fault-tolerance/04-CONTEXT.md
@.planning/phases/04-fault-tolerance/04-RESEARCH.md

# Prior plan summaries
@.planning/phases/04-fault-tolerance/04-01-SUMMARY.md
@.planning/phases/04-fault-tolerance/04-02-SUMMARY.md

# Network fault infrastructure
@src/testing/cluster/network.zig
@src/testing/packet_simulator.zig
@src/vsr/replica_test.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add network partition tests (FAULT-05)</name>
  <files>src/vsr/fault_tolerance_test.zig</files>
  <action>
Add FAULT-05 tests to the existing fault_tolerance_test.zig file.

For FAULT-05 (Network partitions don't cause data loss):

Per CONTEXT.md: "Network timeout handling: Fast failure with automatic leader re-election - assume leader is dead, elect new one quickly"

1. Create test "FAULT-05: network partition isolates minority without data loss (R=3)"
   - Create 3-node cluster, commit initial operations
   - Partition one replica (B2) using drop_all(.__, .bidirectional)
   - Continue committing operations (majority 2/3 can still commit)
   - Verify: majority replicas have new commits
   - Verify: partitioned replica is behind but has no data loss
   - Heal partition using pass_all(.__, .bidirectional)
   - Run until convergence
   - Verify: all replicas converge to same state

2. Create test "FAULT-05: network partition of primary triggers re-election"
   - Partition the primary (A0) from backups
   - Verify: backups elect new leader
   - Continue operations with new leader
   - Heal partition
   - Verify: old primary catches up, no data loss

3. Create test "FAULT-05: asymmetric partition (send-only)"
   - Create partition where replica can send but not receive
   - Use drop_all with .incoming direction only
   - Verify: cluster handles asymmetric partition correctly
   - Heal and verify convergence

4. Create test "FAULT-05: asymmetric partition (receive-only)"
   - Create partition where replica can receive but not send
   - Use drop_all with .outgoing direction only
   - Verify: cluster handles asymmetric partition correctly
   - Heal and verify convergence

5. Create test "FAULT-05: repeated partition and heal cycles"
   - Partition, commit, heal, repeat multiple times
   - Verify: no data loss across multiple partition cycles
   - Verify: state remains consistent

Implementation note: Use the existing drop_all/pass_all methods from TestReplicas. Test both .bidirectional and directional (.incoming, .outgoing) partitions per RESEARCH.md pitfall about asymmetric failures.
  </action>
  <verify>
```bash
./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-05"
```
All FAULT-05 tests should pass.
  </verify>
  <done>
Five FAULT-05 tests validate network partition handling without data loss.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add packet loss and latency tests (FAULT-06)</name>
  <files>src/vsr/fault_tolerance_test.zig</files>
  <action>
Add FAULT-06 tests to the existing fault_tolerance_test.zig file.

For FAULT-06 (Packet loss and latency spikes don't cause corruption):

The network options in TestContext.init allow configuring:
- one_way_delay_mean / one_way_delay_min: latency settings
- path_clog_duration_mean / path_clog_probability: temporary path blockage

1. Create test "FAULT-06: packet loss doesn't cause data corruption (R=3)"
   - Configure network with significant packet loss probability
   - Create cluster with lossy network settings in network options
   - Commit operations
   - Verify: all operations eventually commit (retries work)
   - Verify: no data corruption (StateChecker validates linearizability)
   - Verify: commit chain integrity (parent checksums match)

2. Create test "FAULT-06: high latency doesn't cause data corruption (R=3)"
   - Configure network with high latency settings
   - Use one_way_delay_mean and one_way_delay_min in network options
   - Commit operations
   - Verify: operations complete despite latency
   - Verify: no data corruption
   - Verify: StateChecker finds no linearizability violations

3. Create test "FAULT-06: latency spikes (path clog) handled correctly"
   - Configure path_clog_probability and path_clog_duration_mean
   - This simulates sudden latency spikes (network congestion)
   - Commit operations during path clog events
   - Verify: cluster maintains consistency
   - Verify: no lost or duplicate operations

4. Create test "FAULT-06: mixed network faults (loss + latency)"
   - Configure both packet loss and latency
   - Commit many operations
   - Verify: all operations complete
   - Verify: commit count matches expected (no duplicates)
   - Verify: linearizability maintained

Implementation note: The existing network infrastructure handles these scenarios. The tests document that ArcherDB is resilient to these conditions. Configure the network options during TestContext.init or modify network settings before operations.

If network options are only settable at init time, create separate TestContext instances with different network configurations.
  </action>
  <verify>
```bash
./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-06"
```
All FAULT-06 tests should pass.
  </verify>
  <done>
Four FAULT-06 tests validate packet loss and latency handling without corruption.
  </done>
</task>

</tasks>

<verification>
```bash
# Run all network fault tests
./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-05"
./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-06"

# Verify tests are discoverable
./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-0" 2>&1 | grep -E "(FAULT-05|FAULT-06)"
```
</verification>

<success_criteria>
1. FAULT-05 tests added and pass (network partition handling)
2. FAULT-06 tests added and pass (packet loss/latency handling)
3. Both symmetric and asymmetric partitions tested
4. Tests verify no data loss after partition heals
5. Tests verify no corruption under lossy/laggy conditions
6. StateChecker linearizability validation leveraged
</success_criteria>

<output>
After completion, create `.planning/phases/04-fault-tolerance/04-03-SUMMARY.md`
</output>
