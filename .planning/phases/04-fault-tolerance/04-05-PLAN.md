---
phase: 04-fault-tolerance
plan: 05
type: execute
wave: 3
depends_on:
  - 04-04
files_modified:
  - .planning/phases/04-fault-tolerance/04-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "All 8 FAULT requirements have passing tests"
    - "Phase verification report documents test coverage"
  artifacts:
    - path: ".planning/phases/04-fault-tolerance/04-VERIFICATION.md"
      provides: "Phase verification report"
      contains: "FAULT-01"
  key_links:
    - from: ".planning/phases/04-fault-tolerance/04-VERIFICATION.md"
      to: "src/vsr/fault_tolerance_test.zig"
      via: "Test results reference"
      pattern: "fault_tolerance_test.zig"
---

<objective>
Create phase verification report documenting all FAULT requirement test coverage.

Purpose: Produce formal verification evidence that Phase 4 is complete. Document which tests validate each requirement, test pass/fail status, and any notes about test approach or limitations.

Output: `.planning/phases/04-fault-tolerance/04-VERIFICATION.md`
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-fault-tolerance/04-CONTEXT.md

# Prior plan summaries
@.planning/phases/04-fault-tolerance/04-01-SUMMARY.md
@.planning/phases/04-fault-tolerance/04-02-SUMMARY.md
@.planning/phases/04-fault-tolerance/04-03-SUMMARY.md
@.planning/phases/04-fault-tolerance/04-04-SUMMARY.md

# Reference: Phase 3 verification format
@.planning/phases/03-data-integrity/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all FAULT tests and collect results</name>
  <files>.planning/phases/04-fault-tolerance/04-VERIFICATION.md</files>
  <action>
Run all FAULT-labeled tests and collect results for the verification report.

1. Run the complete test suite:
   ```bash
   ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-"
   ```

2. For each FAULT requirement, run filtered tests:
   ```bash
   ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-01"
   ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-02"
   # ... etc for FAULT-03 through FAULT-08
   ```

3. Count total FAULT-labeled tests

4. Note any test failures or issues

5. Create the verification report with format:

```markdown
# Phase 4: Fault Tolerance - Verification Report

**Phase:** 04-fault-tolerance
**Verified:** [date]
**Status:** [PASSED/FAILED]

## Summary

- **Requirements:** 8 (FAULT-01 through FAULT-08)
- **Tests:** [N] FAULT-labeled tests
- **Pass rate:** [X]/[N] (100%)

## Requirements Coverage

| Requirement | Description | Tests | Status |
|-------------|-------------|-------|--------|
| FAULT-01 | Process crash (SIGKILL) survival | [N] | PASS |
| FAULT-02 | Power loss survival | [N] | PASS |
| FAULT-03 | Disk read error handling | [N] | PASS |
| FAULT-04 | Full disk handling | [N] | PASS |
| FAULT-05 | Network partition handling | [N] | PASS |
| FAULT-06 | Packet loss/latency handling | [N] | PASS |
| FAULT-07 | Corrupted log entry recovery | [N] | PASS |
| FAULT-08 | Recovery time < 60 seconds | [N] | PASS |

## Test Details

### FAULT-01: Process Crash Survival
- `FAULT-01: process crash (SIGKILL) survives without data loss (R=3)` - PASS
- `FAULT-01: process crash during pending writes (R=3)` - PASS
- `FAULT-01: multiple sequential crashes (R=3)` - PASS

[Continue for each requirement...]

## Test Execution

**Command:** `./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-"`
**Date:** [date]
**Duration:** [time]
**Result:** [N] passed, 0 failed

## Notes

[Any notes about test approach, limitations, or edge cases]

## Verification Evidence

Test file: `src/vsr/fault_tolerance_test.zig`
Test infrastructure: Deterministic Cluster framework with fixed seed (42)
```
  </action>
  <verify>
```bash
# Verify report exists
cat .planning/phases/04-fault-tolerance/04-VERIFICATION.md | head -30

# Verify all 8 requirements are documented
grep -c "FAULT-0" .planning/phases/04-fault-tolerance/04-VERIFICATION.md
```
  </verify>
  <done>
04-VERIFICATION.md created with complete test results and requirement coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md and ROADMAP.md for phase completion</name>
  <files>.planning/STATE.md</files>
  <action>
Update project tracking documents to reflect Phase 4 completion.

1. Update STATE.md:
   - Change current position to Phase 4 complete
   - Add Phase 4 completion status section
   - Update progress percentage
   - Add any new decisions from this phase

2. Update ROADMAP.md:
   - Mark Phase 4 plans as complete
   - Update progress table
   - Add completion date

3. Format following the existing patterns from Phase 3 completion.
  </action>
  <verify>
```bash
# Verify STATE.md updated
grep "Phase 4" .planning/STATE.md

# Verify ROADMAP.md updated
grep -A5 "Phase 4:" .planning/ROADMAP.md | head -10
```
  </verify>
  <done>
STATE.md and ROADMAP.md updated to reflect Phase 4 completion.
  </done>
</task>

</tasks>

<verification>
```bash
# Verify phase completion artifacts
ls -la .planning/phases/04-fault-tolerance/

# Verify all plans have summaries
ls .planning/phases/04-fault-tolerance/*-SUMMARY.md

# Verify verification report exists
cat .planning/phases/04-fault-tolerance/04-VERIFICATION.md | grep "Status:"

# Run final test verification
./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "FAULT-"
```
</verification>

<success_criteria>
1. 04-VERIFICATION.md created with complete test results
2. All 8 FAULT requirements documented with test counts
3. STATE.md updated with Phase 4 completion status
4. ROADMAP.md updated with Phase 4 completion
5. All FAULT tests pass in final verification run
</success_criteria>

<output>
After completion, create `.planning/phases/04-fault-tolerance/04-05-SUMMARY.md`
</output>
