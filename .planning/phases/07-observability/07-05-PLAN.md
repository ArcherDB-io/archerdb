---
phase: 07-observability
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - .planning/phases/07-observability/07-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All 8 OBS requirements mapped to implementation evidence"
    - "Verification report documents pass/fail for each requirement"
    - "OBS-04 includes evidence of trace propagation across replicas"
    - "REQUIREMENTS.md updated with OBS requirement status"
    - "STATE.md reflects Phase 7 completion"
  artifacts:
    - path: ".planning/phases/07-observability/07-VERIFICATION.md"
      provides: "Phase verification report with requirement coverage"
      contains: "OBS-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement statuses"
      contains: "OBS-01.*PASS"
  key_links:
    - from: "07-VERIFICATION.md"
      to: "src/archerdb/metrics.zig"
      via: "requirement evidence"
      pattern: "metrics.zig"
    - from: "07-VERIFICATION.md"
      to: "src/archerdb/observability/correlation.zig"
      via: "OBS-04 trace propagation evidence"
      pattern: "toTraceparent|newChild|W3C|B3"
---

<objective>
Create phase verification report and update project tracking documents.

Purpose: Document requirement coverage, verify all OBS requirements are satisfied (with special attention to OBS-04 cross-replica trace correlation), and mark Phase 7 complete.

Output: Verification report, updated REQUIREMENTS.md and STATE.md.
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-observability/07-CONTEXT.md

# Prior plan summaries
@.planning/phases/07-observability/07-01-SUMMARY.md
@.planning/phases/07-observability/07-02-SUMMARY.md
@.planning/phases/07-observability/07-03-SUMMARY.md
@.planning/phases/07-observability/07-04-SUMMARY.md

# Key source files for OBS-04 verification
@src/archerdb/observability/correlation.zig
@src/archerdb/observability/json_logger.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase verification report with OBS-04 trace propagation evidence</name>
  <files>.planning/phases/07-observability/07-VERIFICATION.md</files>
  <action>
Create verification report documenting how each OBS requirement is satisfied.

**CRITICAL for OBS-04:** Must verify distributed tracing correlates requests across replicas by documenting:
1. correlation.zig provides W3C Trace Context and B3 header parsing (fromTraceparent, fromB3Headers)
2. correlation.zig provides trace context propagation (toTraceparent for outgoing headers)
3. correlation.zig provides child span creation (newChild inherits trace_id across replicas)
4. json_logger.zig includes replica_id in log output alongside trace_id
5. Thread-local context storage (setCurrent/getCurrent) enables propagation through request handling

**Verification commands for OBS-04:**
- `grep -c "toTraceparent\|fromTraceparent\|fromB3Headers" src/archerdb/observability/correlation.zig` (should show propagation methods)
- `grep -c "replica_id" src/archerdb/observability/correlation.zig` (should show replica correlation)
- `grep "newChild" src/archerdb/observability/correlation.zig` (should show child span inherits trace_id)

Structure:
```markdown
# Phase 7: Observability - Verification Report

**Verified:** [date]
**Status:** PASS / PARTIAL / FAIL

## Requirement Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| OBS-01 | Prometheus metrics export key performance indicators | PASS | metrics.zig Registry with 150+ metrics |
| OBS-02 | Grafana dashboard shows cluster health | PASS | archerdb-unified-overview.json |
| OBS-03 | Prometheus alerts fire for critical conditions | PASS | latency.yml, disk.yml, storage.yml, memory.yml |
| OBS-04 | Distributed tracing correlates requests across replicas | PASS | correlation.zig W3C/B3 propagation + replica_id |
| OBS-05 | Structured JSON logs include trace IDs | PASS | json_logger.zig with shortTraceId |
| OBS-06 | Log aggregation configured (stdout/file) | PASS | JsonLogHandler outputs to stderr/file |
| OBS-07 | Metrics include 99th/999th percentile latencies | PASS | LatencyHistogram getExtendedStats() |
| OBS-08 | Resource usage metrics (CPU, memory, disk) exported | PASS | collectProcessMetrics() in metrics_server.zig |

## Detailed Evidence

### OBS-01: Prometheus metrics export key performance indicators
- **File:** `src/archerdb/metrics.zig`
- **Evidence:** Registry contains counters, gauges, histograms for write/read/delete operations, latencies, cache hits, replication lag
- **Verification:** `grep -c "pub var" src/archerdb/metrics.zig` shows 100+ metric definitions
- **Status:** PASS

### OBS-02: Grafana dashboard shows cluster health
- **File:** `observability/grafana/dashboards/archerdb-unified-overview.json`
- **Evidence:** Green/yellow/red status indicators per node, throughput+latency dual Y-axis, resource usage
- **Verification:** Dashboard exists and is valid JSON
- **Status:** PASS

### OBS-03: Prometheus alerts fire for critical conditions
- **Files:** `observability/prometheus/alerts/latency.yml`, `disk.yml`, `storage.yml`, `memory.yml`
- **Evidence:** Alert rules with severity levels, runbook URLs, appropriate thresholds
- **Verification:** YAML files are valid, contain alert rules
- **Status:** PASS

### OBS-04: Distributed tracing correlates requests across replicas
- **File:** `src/archerdb/observability/correlation.zig`
- **Evidence:** Full W3C Trace Context and B3 header support for cross-service trace propagation
- **Cross-replica correlation mechanisms:**
  1. `fromTraceparent()` / `fromB3Headers()`: Parse incoming trace context from other replicas
  2. `toTraceparent()`: Format trace context for outgoing requests to other replicas
  3. `newChild()`: Creates child span that inherits trace_id (line 275-286), ensuring same trace across replicas
  4. `replica_id` field (line 66): Distinguishes spans from different replicas within same trace
  5. Thread-local `setCurrent()`/`getCurrent()`: Propagates context through request handling path
- **Verification commands:**
  - `grep -c "toTraceparent" src/archerdb/observability/correlation.zig` >= 1
  - `grep -c "fromTraceparent\|fromB3Headers" src/archerdb/observability/correlation.zig` >= 2
  - `grep "replica_id" src/archerdb/observability/correlation.zig` shows replica correlation
  - `grep -A5 "newChild" src/archerdb/observability/correlation.zig` shows trace_id inheritance
- **How it works across replicas:**
  - Incoming request: Parse traceparent/B3 headers -> set replica_id -> setCurrent()
  - Forwarding to replica: Create child span -> toTraceparent() -> include in outgoing request
  - All replicas log with same trace_id, different replica_id -> correlatable
- **Status:** PASS

### OBS-05: Structured JSON logs include trace IDs
- **File:** `src/archerdb/observability/json_logger.zig`
- **Evidence:** JSON log output includes trace_id, span_id, request_id, replica_id fields
- **Verification:** `grep "trace_id\|request_id\|replica_id" src/archerdb/observability/json_logger.zig`
- **Status:** PASS

### OBS-06: Log aggregation configured (stdout/file)
- **File:** `src/archerdb/observability/json_logger.zig`
- **Evidence:** JsonLogHandler outputs to configurable destinations (stderr/file)
- **Status:** PASS

### OBS-07: Metrics include 99th/999th percentile latencies
- **File:** `src/archerdb/metrics.zig`
- **Evidence:** LatencyHistogram with getExtendedStats() providing P50, P90, P99, P99.9
- **Verification:** `grep "getExtendedStats\|P99\|percentile" src/archerdb/metrics.zig`
- **Status:** PASS

### OBS-08: Resource usage metrics (CPU, memory, disk) exported
- **File:** `src/archerdb/metrics_server.zig`
- **Evidence:** collectProcessMetrics() exports CPU, memory, file descriptor metrics
- **Verification:** `grep "process_cpu\|process_memory\|process_open" src/archerdb/metrics_server.zig`
- **Status:** PASS

## Test Execution

| Test | Command | Result |
|------|---------|--------|
| Build | `./zig/zig build -j4 -Dconfig=lite check` | PASS |
| Metrics grep | `grep -c "pub var" src/archerdb/metrics.zig` | 100+ |
| Dashboard valid | `python3 -c "import json; json.load(...)"` | PASS |
| Alert YAML valid | `python3 -c "import yaml; yaml.safe_load(...)"` | PASS |
| OBS-04 propagation methods | `grep -c "toTraceparent\|fromTraceparent" src/archerdb/observability/correlation.zig` | >= 3 |
| OBS-04 replica correlation | `grep -c "replica_id" src/archerdb/observability/correlation.zig` | >= 5 |

## Summary

Phase 7 complete. All 8 OBS requirements satisfied.
- 4 plans executed successfully
- Metrics infrastructure enhanced with CONTEXT.md histogram buckets
- Unified dashboard created per user decisions
- Alert rules with aggressive latency thresholds deployed
- Runtime log level toggle implemented
- Distributed tracing fully supports cross-replica correlation via W3C/B3 propagation
```
  </action>
  <verify>
File exists: `test -f .planning/phases/07-observability/07-VERIFICATION.md && echo "OK"`
Contains all requirements: `grep -c "OBS-0[1-8]" .planning/phases/07-observability/07-VERIFICATION.md` (should be >= 8)
OBS-04 has propagation evidence: `grep -c "toTraceparent\|fromTraceparent\|replica_id\|newChild" .planning/phases/07-observability/07-VERIFICATION.md` (should be >= 4)
  </verify>
  <done>
Verification report exists with all 8 OBS requirements documented. OBS-04 specifically includes evidence of trace context propagation across replicas via W3C/B3 headers, newChild() trace_id inheritance, and replica_id correlation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
1. In REQUIREMENTS.md, update OBS requirement statuses:
   - Find lines with OBS-01 through OBS-08
   - Change `- [ ]` to `- [x]` for each
   - Update status table: `Pending` -> `PASS`

2. In STATE.md:
   - Update "Current Position" section:
     - Phase: 7 of 10 (Observability) - COMPLETE
     - Plan: 5 of 5 in current phase (complete)
   - Update progress bar and percentage
   - Add Phase 7 Completion Status section (similar to Phase 6)
   - Update "Next" to point to Phase 8

3. In ROADMAP.md:
   - Mark Phase 7 complete: `- [x] **Phase 7: Observability**`
   - Update Progress table: Phase 7 status to "Complete"
   - Add completion date
  </action>
  <verify>
Requirements updated: `grep "OBS-01" .planning/REQUIREMENTS.md | grep -q "x"` (should show checked)
State updated: `grep "Phase 7" .planning/STATE.md | head -1`
  </verify>
  <done>
REQUIREMENTS.md shows all OBS requirements as PASS. STATE.md shows Phase 7 complete. ROADMAP.md updated.
  </done>
</task>

</tasks>

<verification>
1. Verification report exists: `test -f .planning/phases/07-observability/07-VERIFICATION.md`
2. All OBS requirements covered: `grep -c "OBS-0[1-8].*PASS" .planning/phases/07-observability/07-VERIFICATION.md` >= 8
3. OBS-04 cross-replica evidence: `grep -c "toTraceparent\|newChild\|replica_id" .planning/phases/07-observability/07-VERIFICATION.md` >= 3
4. REQUIREMENTS.md updated: `grep -c "\[x\].*OBS" .planning/REQUIREMENTS.md` >= 8
5. STATE.md shows Phase 7 complete: `grep -q "Phase 7.*COMPLETE\|Phase: 7.*complete" .planning/STATE.md`
</verification>

<success_criteria>
- 07-VERIFICATION.md exists with all 8 OBS requirements documented
- Each requirement has status (PASS expected) and evidence
- OBS-04 specifically documents cross-replica trace propagation evidence:
  - W3C/B3 header parsing (fromTraceparent, fromB3Headers)
  - Trace context propagation (toTraceparent for outgoing)
  - Child span creation (newChild inherits trace_id)
  - Replica correlation (replica_id field)
- REQUIREMENTS.md checkboxes updated for OBS-01 through OBS-08
- STATE.md reflects Phase 7 completion
- ROADMAP.md marks Phase 7 complete with date
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability/07-05-SUMMARY.md`
</output>
