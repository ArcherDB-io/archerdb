---
phase: 18-ci-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/sdk-smoke.yml
  - .github/workflows/sdk-pr.yml
  - .github/workflows/sdk-nightly.yml
  - .github/workflows/benchmark-weekly.yml
autonomous: true

must_haves:
  truths:
    - "SDK tests run automatically on every PR"
    - "Smoke tests complete in <5 minutes and gate PR merges"
    - "Nightly suite runs all 6 SDKs across multiple topologies"
    - "Weekly benchmark suite runs with historical tracking"
    - "Benchmark regressions >10% trigger alerts and fail workflow"
  artifacts:
    - path: ".github/workflows/sdk-smoke.yml"
      provides: "Smoke tests on every push"
      contains: "junit-xml"
    - path: ".github/workflows/sdk-pr.yml"
      provides: "Full PR test suite"
      contains: "matrix:"
    - path: ".github/workflows/sdk-nightly.yml"
      provides: "Nightly multi-node tests"
      contains: "node_count:"
    - path: ".github/workflows/benchmark-weekly.yml"
      provides: "Weekly benchmark automation"
      contains: "cron: '0 2 * * 0'"
  key_links:
    - from: ".github/workflows/sdk-pr.yml"
      to: "mikepenz/action-junit-report"
      via: "uses step"
      pattern: "mikepenz/action-junit-report"
    - from: ".github/workflows/benchmark-weekly.yml"
      to: "benchmark-action/github-action-benchmark"
      via: "uses step"
      pattern: "benchmark-action/github-action-benchmark"
---

<objective>
Enhance existing CI workflows to meet all CI requirements (CI-01 through CI-06).

Purpose: Automated CI pipelines enable continuous quality assurance with proper test reporting and benchmark tracking.
Output: Four enhanced/new workflow files providing comprehensive CI coverage.
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-ci-documentation/18-CONTEXT.md
@.planning/phases/18-ci-documentation/18-RESEARCH.md
@.github/workflows/sdk-smoke.yml
@.github/workflows/sdk-pr.yml
@.github/workflows/sdk-nightly.yml
@.github/workflows/benchmark.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance SDK smoke and PR workflows with JUnit XML reporting</name>
  <files>
    - .github/workflows/sdk-smoke.yml
    - .github/workflows/sdk-pr.yml
  </files>
  <action>
Update sdk-smoke.yml:
1. Add all 6 SDKs to smoke tests (currently only Python and Node.js)
2. Add JUnit XML output for each SDK:
   - Python: `pytest --junit-xml=test-results/python.xml`
   - Node.js: Add jest-junit configuration
   - Go: Use go-junit-report
   - Java: Maven surefire reports
   - C: Custom script outputting JUnit format (or skip if not implemented)
   - Zig: Custom script outputting JUnit format (or skip if not implemented)
3. Add mikepenz/action-junit-report@v5 step with fail_on_failure: true
4. Add actions/upload-artifact@v4 for test results
5. Keep strict 5-minute timeout (CI-02)
6. Set fail-fast: true per CONTEXT.md

Update sdk-pr.yml:
1. Add C and Zig SDKs to matrix (currently only python, nodejs, go, java)
2. Add JUnit XML output for each SDK (same pattern as smoke)
3. Add mikepenz/action-junit-report@v5 step
4. Add actions/upload-artifact@v4 with name: test-results-${{ matrix.sdk }}
5. Keep fail-fast: true per CONTEXT.md
6. Keep 15-minute timeout

For both workflows:
- Use `if: always()` for report publishing steps (publish even if tests fail)
- Set artifact retention-days: 30
  </action>
  <verify>
Run `cat .github/workflows/sdk-smoke.yml | grep -E "(junit|mikepenz|upload-artifact)"` shows expected patterns.
Run `cat .github/workflows/sdk-pr.yml | grep -E "(c|zig)" | head -5` shows C/Zig in matrix.
  </verify>
  <done>
Both smoke and PR workflows have JUnit XML reporting, all 6 SDKs tested, and test results uploaded as artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance nightly workflow and create weekly benchmark workflow</name>
  <files>
    - .github/workflows/sdk-nightly.yml
    - .github/workflows/benchmark-weekly.yml
  </files>
  <action>
Update sdk-nightly.yml:
1. Add JUnit XML output for all SDK tests (same pattern as PR workflow)
2. Add mikepenz/action-junit-report@v5 step for each SDK
3. Add combined test results artifact upload
4. Set fail-fast: true per CONTEXT.md (stop immediately on first test failure)
5. Keep 2-hour timeout
6. Use ubuntu-latest-4-cores runner per CONTEXT.md decision

Create benchmark-weekly.yml (NEW):
1. Schedule: cron '0 2 * * 0' (Sunday 2 AM UTC)
2. Also allow workflow_dispatch for manual triggers
3. Use concurrency group 'benchmark-weekly' with cancel-in-progress: false
4. Use ubuntu-latest-8-cores runner per CONTEXT.md decision
5. Steps:
   a. Checkout with fetch-depth: 0
   b. Setup Python 3.11
   c. Install test_infrastructure requirements
   d. Build ArcherDB release: `./zig/zig build -Drelease`
   e. Run benchmark suite: `python test_infrastructure/benchmarks/cli.py run --full-suite --output-json benchmark-results.json`
   f. Use benchmark-action/github-action-benchmark@v1:
      - tool: 'customSmallerIsBetter'
      - output-file-path: benchmark-results.json
      - auto-push: true
      - alert-threshold: '110%' (10% regression per Phase 15 decision)
      - comment-on-alert: true
      - fail-on-alert: true
      - benchmark-data-dir-path: 'benchmarks/history'
   g. Commit JSON to benchmarks/history/YYYY-MM-DD.json
   h. Upload benchmark-results.json as artifact with retention-days: 90
7. Set timeout-minutes: 180 (3 hours)
  </action>
  <verify>
Run `cat .github/workflows/sdk-nightly.yml | grep "junit" | wc -l` shows JUnit XML integration.
Run `cat .github/workflows/benchmark-weekly.yml | grep -E "(cron|benchmark-action)"` shows schedule and benchmark action.
Run `ls .github/workflows/benchmark-weekly.yml` confirms file exists.
  </verify>
  <done>
Nightly workflow has JUnit reporting with fail-fast enabled, and weekly benchmark workflow exists with github-action-benchmark integration and 10% regression threshold.
  </done>
</task>

</tasks>

<verification>
1. All workflow files are valid YAML: `python -c "import yaml; yaml.safe_load(open('.github/workflows/sdk-smoke.yml'))"`
2. JUnit XML patterns present in all test workflows
3. Weekly benchmark workflow has correct schedule and regression alerting
4. CI-01: SDK tests on every PR (sdk-pr.yml triggers on pull_request)
5. CI-02: Smoke <5 min (sdk-smoke.yml timeout-minutes: 5)
6. CI-03: Nightly multi-node (sdk-nightly.yml with node_count matrix)
7. CI-04: Weekly benchmark (benchmark-weekly.yml with schedule)
8. CI-05: Historical tracking (github-action-benchmark with auto-push)
9. CI-06: Failures block (fail-on-alert: true, fail_on_failure: true)
10. All tiers use fail-fast: true per CONTEXT.md
</verification>

<success_criteria>
- [ ] sdk-smoke.yml has all 6 SDKs with JUnit XML output and <5 min timeout
- [ ] sdk-pr.yml has all 6 SDKs (including C, Zig) with JUnit XML output and <15 min timeout
- [ ] sdk-nightly.yml has JUnit XML output for all SDKs with 2h timeout and fail-fast: true
- [ ] benchmark-weekly.yml exists with Sunday 2 AM schedule
- [ ] benchmark-weekly.yml uses github-action-benchmark with 110% threshold
- [ ] All workflows have artifact upload for test results
- [ ] All workflows are valid YAML
- [ ] All test workflows use fail-fast: true per CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/18-ci-documentation/18-01-SUMMARY.md`
</output>
