---
phase: 03-data-integrity
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - .planning/phases/03-data-integrity/03-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "All DATA requirements have passing tests"
    - "Verification report documents test coverage"
    - "Phase 3 is ready for sign-off"
  artifacts:
    - path: ".planning/phases/03-data-integrity/03-VERIFICATION.md"
      provides: "Phase 3 verification report"
      contains: "DATA-01"
  key_links:
    - from: ".planning/phases/03-data-integrity/03-VERIFICATION.md"
      to: "src/vsr/data_integrity_test.zig"
      via: "documents test results"
      pattern: "DATA-"
---

<objective>
Run all DATA tests and create verification report documenting coverage for all DATA-01 through DATA-09 requirements.

Purpose: Provide evidence that all data integrity requirements are validated by passing tests. Document any gaps or limitations.

Output: `.planning/phases/03-data-integrity/03-VERIFICATION.md` with test results and coverage analysis
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-integrity/03-CONTEXT.md

# Prior plan summaries for context
@.planning/phases/03-data-integrity/03-01-SUMMARY.md
@.planning/phases/03-data-integrity/03-02-SUMMARY.md
@.planning/phases/03-data-integrity/03-03-SUMMARY.md
@.planning/phases/03-data-integrity/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run comprehensive DATA test suite</name>
  <files>none (test execution only)</files>
  <action>
Execute all DATA tests and capture results.

1. Run all DATA-xx labeled tests:
   ```bash
   ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "DATA-" 2>&1 | tee /tmp/data-tests.log
   ```

2. Run existing replica_test.zig corruption tests (additional DATA-01, 02, 03 coverage):
   ```bash
   ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "Cluster: recovery" 2>&1 | tee /tmp/recovery-tests.log
   ```

3. Run backup/restore integration tests:
   ```bash
   ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "Integration:" 2>&1 | tee /tmp/integration-tests.log
   ```

4. Run checksum unit tests (DATA-03):
   ```bash
   ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "checksum" 2>&1 | tee /tmp/checksum-tests.log
   ```

5. Optional: Short VOPR run for concurrent write validation (DATA-05):
   ```bash
   ./zig/zig build vopr -Dvopr-state-machine=testing -- --lite --ticks-max-requests=5000 42 2>&1 | tee /tmp/vopr-test.log
   ```

Capture pass/fail status for each test category.
  </action>
  <verify>
All test commands complete successfully. Check exit codes:
```bash
echo $?  # Should be 0 for each test run
```
  </verify>
  <done>
All DATA tests executed and results captured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create verification report</name>
  <files>.planning/phases/03-data-integrity/03-VERIFICATION.md</files>
  <action>
Create the verification report documenting test coverage for all DATA requirements.

Report structure:
```markdown
# Phase 3: Data Integrity - Verification Report

**Generated:** [date]
**Status:** [PASS/FAIL/PARTIAL]

## Executive Summary

[1-2 sentences on overall status]

## Requirements Coverage

| Requirement | Test(s) | Status | Notes |
|-------------|---------|--------|-------|
| DATA-01: WAL replay | DATA-01: WAL replay tests | PASS | Validates recovery from corruption |
| DATA-02: Checkpoint/restore | DATA-02: checkpoint tests | PASS | Grid corruption recovery |
| DATA-03: Checksums | DATA-03: checksum tests | PASS | Single-bit flip detection |
| DATA-04: Read-your-writes | DATA-04: consistency tests | PASS | StateChecker validates |
| DATA-05: Concurrent writes | DATA-05: concurrency tests | PASS | Multi-client safety |
| DATA-06: Torn writes | DATA-06: torn write tests | PASS | Header corruption handling |
| DATA-07: Backup snapshot | DATA-07: backup tests | PASS | Queue consistency |
| DATA-08: Restore full state | DATA-08: restore tests | PASS | Stats verification |
| DATA-09: PITR | DATA-09: PITR tests | PASS | Parsing and config |

## Test Results Summary

### Unit Tests
- DATA-0x tests: X/X passed
- Cluster recovery tests: X/X passed
- Checksum tests: X/X passed
- Backup/restore tests: X/X passed

### Integration Tests
- Backup integration: X/X passed

### VOPR (Optional)
- Concurrent write stress: PASS/SKIPPED

## Coverage Analysis

### Well Covered
- [list strengths]

### Limitations
- [list any gaps or caveats]

## Recommendations

1. [Any follow-up items]

## Sign-off

Phase 3: Data Integrity is **VERIFIED COMPLETE**.
All 9 DATA requirements have passing test coverage.
```

Fill in actual test results from Task 1.
  </action>
  <verify>
```bash
cat .planning/phases/03-data-integrity/03-VERIFICATION.md | head -30
```
Report exists with filled-in test results.
  </verify>
  <done>
Verification report created documenting all DATA requirement coverage.
  </done>
</task>

</tasks>

<verification>
```bash
# Verify report exists and has content
test -f .planning/phases/03-data-integrity/03-VERIFICATION.md && echo "Report exists"
grep -c "DATA-0" .planning/phases/03-data-integrity/03-VERIFICATION.md
# Should show 9+ occurrences (one per requirement at minimum)
```
</verification>

<success_criteria>
1. All DATA-xx tests executed
2. All cluster recovery tests executed
3. Verification report created
4. Each DATA requirement (01-09) has documented test coverage
5. Report clearly states PASS/FAIL status for each requirement
6. Any limitations or gaps are documented
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-integrity/03-05-SUMMARY.md`
</output>
