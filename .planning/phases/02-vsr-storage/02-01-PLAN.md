---
phase: 02-vsr-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vsr.zig
  - src/vsr/message_header.zig
  - src/vsr/replica.zig
  - src/vsr/journal.zig
autonomous: true

must_haves:
  truths:
    - "Deprecated message types (12, 21, 22, 23) are rejected with clear error"
    - "Snapshot verification is enabled in manifest block validation"
    - "Journal prepare checksums assertion is re-enabled or root cause documented"
    - "Unit tests pass for all VSR protocol edge cases"
  artifacts:
    - path: "src/vsr.zig"
      provides: "Reserved enum slots with documentation"
      contains: "// RESERVED: Never reuse"
    - path: "src/vsr/message_header.zig"
      provides: "Snapshot verification enabled"
      contains: "snapshot != 0"
    - path: "src/vsr/replica.zig"
      provides: "Clean deprecated message rejection"
  key_links:
    - from: "src/vsr/replica.zig"
      to: "src/vsr/message_header.zig"
      via: "deprecated message type handling"
      pattern: "deprecated_.*unreachable"
---

<objective>
Fix VSR protocol issues: remove deprecated message type handlers, enable snapshot verification, and investigate journal assertion.

Purpose: The VSR consensus layer must be verifiably correct. Disabled verification checks and commented-out assertions represent technical debt that undermines confidence in correctness.

Output:
- Deprecated message types cleaned up (enum slots reserved forever, handlers removed)
- Snapshot verification enabled (was disabled at message_header.zig:1623)
- Journal assertion investigated and either re-enabled or root cause documented
- All existing VSR tests continue to pass
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vsr-storage/02-CONTEXT.md
@.planning/phases/02-vsr-storage/02-RESEARCH.md

# Key source files
@src/vsr.zig
@src/vsr/message_header.zig
@src/vsr/replica.zig
@src/vsr/journal.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up deprecated VSR message types</name>
  <files>
    src/vsr.zig
    src/vsr/message_header.zig
    src/vsr/replica.zig
  </files>
  <action>
    Remove handler code for deprecated_12, deprecated_21, deprecated_22, deprecated_23 while keeping enum slots reserved:

    1. In `src/vsr.zig:292-295`:
       - Keep the enum values (12, 21, 22, 23) reserved forever
       - Add clear comment: `// RESERVED: These message IDs were used in prior protocol versions and must never be reused for wire compatibility`
       - Document what each deprecated type was: deprecated_12=start_view without checkpoint, deprecated_21=request_sync_checkpoint, deprecated_22=sync_checkpoint, deprecated_23=start_view with older CheckpointState

    2. In `src/vsr/message_header.zig`:
       - Keep the Deprecated struct at lines 102-105 mapping to validate deprecated messages
       - Ensure invalid_header() returns clear error message: "deprecated message type {id} - protocol violation"
       - Lines 235-238: Keep in pattern match, ensure they reject cleanly

    3. In `src/vsr/replica.zig`:
       - Lines 1815-1818: Already `unreachable` - verify this is correct behavior
       - Lines 9094-9097 and 9227-9230: Verify these pattern matches correctly exclude deprecated types
       - Search for any other handlers that might process these types and remove

    Wire compatibility: The enum ordinals MUST remain reserved (never reuse slot 12, 21, 22, 23). Only remove logic that would "handle" these messages - they should always be rejected at the message_header validation layer.
  </action>
  <verify>
    ./zig/zig build test:unit -- --test-filter "message" 2>&1 | head -50
    # Grep for deprecated handling to ensure cleanup is complete:
    grep -r "deprecated_12\|deprecated_21\|deprecated_22\|deprecated_23" src/vsr/ --include="*.zig" | grep -v "RESERVED\|enum\|unreachable"
  </verify>
  <done>
    - Deprecated enum slots documented with "RESERVED: Never reuse" comments
    - No handler code attempts to process deprecated message types (only rejection)
    - Wire compatibility preserved (enum ordinals unchanged)
    - All message-related tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Investigate and enable snapshot verification</name>
  <files>
    src/vsr/message_header.zig
    src/vsr/superblock.zig
    src/lsm/manifest.zig
    src/lsm/manifest_log.zig
  </files>
  <action>
    Investigate the disabled snapshot verification at message_header.zig:1623:

    1. **Understand the issue**: The TODO says "When manifest blocks include a snapshot, verify that snapshot != 0". This suggests manifest blocks should have snapshot numbers but the verification is disabled.

    2. **Trace the data flow**:
       - Find where Block headers are created (likely in manifest_log.zig or grid.zig)
       - Find where snapshot numbers are set
       - Determine why the check was disabled (look in git history if helpful)

    3. **Investigate root cause**:
       - Look at Block struct definition and how snapshot field is populated
       - Check if manifest blocks are being created with snapshot=0 incorrectly
       - Or if the validation assumption is wrong (some blocks legitimately have snapshot=0)

    4. **Fix or document**:
       - If the fix is straightforward: Enable the verification by uncommenting/implementing the check
       - If complex: Document the root cause in a code comment explaining what would need to change
       - If the assumption is wrong: Update the TODO with correct understanding

    5. **Add test coverage**:
       - Add unit test that verifies manifest blocks have valid snapshot numbers
       - Test should fail if verification is disabled and blocks have snapshot=0 incorrectly

    Note: This is investigative work. The goal is understanding and either fixing or clearly documenting why the check is disabled. Do not enable verification that breaks valid use cases.
  </action>
  <verify>
    ./zig/zig build test:unit -- --test-filter "manifest\|block\|snapshot" 2>&1 | head -100
    # Verify the TODO is addressed (either fixed or documented with explanation)
    grep -A5 "snapshot.*!=.*0\|snapshot verification" src/vsr/message_header.zig
  </verify>
  <done>
    - Root cause of disabled snapshot verification understood and documented
    - Either: verification enabled with passing tests, OR clear documentation explaining why it remains disabled and what would need to change
    - If enabled: manifest block tests verify snapshot != 0 for blocks that should have snapshots
  </done>
</task>

<task type="auto">
  <name>Task 3: Investigate journal prepare checksums assertion</name>
  <files>
    src/vsr/journal.zig
  </files>
  <action>
    Investigate the commented-out assertion at journal.zig:303:

    1. **Read the context**: The TODO says "Fix this assertion" for what appears to be a write_ahead_log_zone_size vs storage.size check.

    2. **Understand the purpose**:
       - Why was this assertion originally added?
       - What condition is it trying to verify?
       - When would it legitimately fail?

    3. **Investigate the failure mode**:
       - Look at how write_ahead_log_zone_size is computed
       - Look at how storage.size is set
       - Determine if there's a valid scenario where the assertion fails

    4. **Fix or document**:
       - If the assertion is valid: Re-enable it and fix whatever causes it to fail
       - If the assertion is incorrect: Remove the TODO and explain why in comments
       - If complex: Document what would need to change to re-enable

    5. **Run VOPR to verify**:
       - Run VOPR with a few seeds to check for journal-related failures
       - If re-enabling the assertion, verify it doesn't cause false positives

    Note: Assertions that are commented out represent uncertainty about correctness. The goal is to either have the assertion or have clear documentation explaining why it's not needed.
  </action>
  <verify>
    ./zig/zig build test:unit -- --test-filter "journal" 2>&1 | head -50
    # Quick VOPR run to verify journal behavior
    ./zig/zig build vopr -Dvopr-state-machine=testing -- --lite --requests-max=100 42
    # Check assertion status
    grep -n "write_ahead_log_zone_size\|TODO.*Fix this assertion" src/vsr/journal.zig | head -10
  </verify>
  <done>
    - Journal assertion investigated with root cause understood
    - Either: assertion re-enabled and passing, OR clear documentation of why it's not applicable
    - VOPR lite run passes without journal-related errors
    - No unexplained TODOs about journal assertions remain
  </done>
</task>

</tasks>

<verification>
All VSR protocol fixes verified:

1. **Deprecated messages**: `grep -r "deprecated_" src/vsr/` shows only reserved enum slots and rejection logic
2. **Snapshot verification**: Either enabled or documented with clear explanation
3. **Journal assertion**: Either enabled or documented with clear explanation
4. **Tests pass**: `./zig/zig build test:unit -- --test-filter "vsr\|message\|journal"` passes
5. **VOPR passes**: `./scripts/run_vopr.sh --seeds "42 97 123"` completes without errors
</verification>

<success_criteria>
- [ ] Deprecated message types (12, 21, 22, 23) have "RESERVED" documentation
- [ ] No handler code processes deprecated message types (only rejection)
- [ ] Snapshot verification at message_header.zig:1623 either enabled or clearly documented
- [ ] Journal assertion at journal.zig:303 either enabled or clearly documented
- [ ] All VSR-related unit tests pass
- [ ] VOPR lite runs pass with seeds 42, 97, 123
</success_criteria>

<output>
After completion, create `.planning/phases/02-vsr-storage/02-01-SUMMARY.md`
</output>
