---
phase: 03-core-geospatial
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/geo_state_machine.zig
  - src/geo_event.zig
autonomous: true

must_haves:
  truths:
    - "Insert GeoEvent stores location with all metadata"
    - "Upsert updates existing entity and creates tombstone for old version"
    - "Delete removes entity from all indexes"
    - "Deleted entity is not retrievable (GDPR compliance)"
    - "UUID query returns correct entity by ID"
    - "TTL expiration removes expired entities"
  artifacts:
    - path: "src/geo_state_machine.zig"
      provides: "Entity operations (insert, upsert, delete, query)"
      exports: ["GeoStateMachine"]
    - path: "src/geo_event.zig"
      provides: "GeoEvent with tombstone support"
      contains: "create_tombstone"
  key_links:
    - from: "src/geo_state_machine.zig"
      to: "src/ram_index.zig"
      via: "RAM index operations"
      pattern: "ram_index.*upsert|ram_index.*remove"
    - from: "src/geo_state_machine.zig"
      to: "src/geo_event.zig"
      via: "GeoEvent creation and tombstones"
      pattern: "GeoEvent|create_tombstone"
---

<objective>
Verify entity operations (insert, upsert, delete, query) work correctly with proper tombstone handling.

Purpose: Entity operations are the core data manipulation interface. Correctness ensures data integrity, GDPR compliance (right to erasure), and proper TTL-based lifecycle management.

Output: Comprehensive tests proving all entity operations work correctly with proper tombstone semantics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-geospatial/03-CONTEXT.md
@.planning/phases/03-core-geospatial/03-RESEARCH.md

@src/geo_state_machine.zig
@src/geo_event.zig
@src/ram_index.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add insert and upsert verification tests</name>
  <files>
    src/geo_state_machine.zig
  </files>
  <action>
Add comprehensive tests for insert and upsert operations:

1. Add test "entity ops: insert stores all fields":
   - Insert GeoEvent with: entity_id, lat, lon, timestamp, group_id, metadata
   - Query by UUID
   - Verify all fields are stored correctly
   - Verify event is in RAM index

2. Add test "entity ops: insert on existing entity returns error":
   - Insert entity A
   - Insert entity A again (same entity_id)
   - Per CONTEXT.md: "Insert on existing entity ID returns error"
   - Verify error returned (not silent overwrite)

3. Add test "entity ops: upsert creates new entry":
   - Upsert entity B (doesn't exist)
   - Verify entity B is created
   - Same as insert for new entities

4. Add test "entity ops: upsert updates existing":
   - Insert entity C at location 1
   - Upsert entity C at location 2 (new lat/lon)
   - Query entity C
   - Verify location is updated to location 2
   - Per CONTEXT.md: "Atomic move for upsert location changes"

5. Add test "entity ops: upsert creates tombstone for old version":
   - Insert entity D at time T1
   - Upsert entity D at time T2
   - Verify tombstone exists for T1 version
   - Per CONTEXT.md: "TTL-based tombstones"

6. Add test "entity ops: LWW semantics":
   - Upsert entity E with timestamp 100
   - Upsert entity E with timestamp 50 (older)
   - Verify entity E has timestamp 100 (newer wins)
   - This is standard LWW (Last-Write-Wins) behavior

Document: Use explicit timestamps for reproducibility, not wall-clock time.
  </action>
  <verify>
```bash
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "entity ops"
```
All insert/upsert tests pass.
  </verify>
  <done>
- Insert stores all GeoEvent fields correctly
- Insert on existing entity returns error
- Upsert creates new or updates existing
- Upsert creates tombstone for old version
- LWW semantics verified
  </done>
</task>

<task type="auto">
  <name>Task 2: Add delete and GDPR compliance tests</name>
  <files>
    src/geo_state_machine.zig
    src/geo_event.zig
  </files>
  <action>
Add comprehensive tests for delete operations and GDPR compliance:

1. Add test "entity ops: delete removes from RAM index":
   - Insert entity F
   - Verify entity F is in RAM index
   - Delete entity F
   - Verify entity F is NOT in RAM index
   - Query by UUID returns NOT_FOUND

2. Add test "entity ops: delete creates tombstone":
   - Insert entity G
   - Delete entity G
   - Verify tombstone is created
   - Tombstone has: entity_id set, deleted flag true

3. Add test "entity ops: GDPR deletion verification":
   - Insert entity H
   - Delete entity H (simulating GDPR erasure request)
   - Attempt various queries:
     - UUID query: NOT_FOUND
     - Radius query at entity's location: does not include entity H
     - Polygon query containing entity: does not include entity H
   - Per CONTEXT.md: "Delete verifiable (entity not retrievable after delete)"

4. Add test "entity ops: batch delete":
   - Insert entities I1, I2, I3
   - Batch delete [I1, I3]
   - Verify I1, I3 not retrievable
   - Verify I2 still retrievable

5. Add test "GeoEvent: tombstone creation":
   - Create normal GeoEvent
   - Call `create_tombstone()`
   - Verify: flags.deleted = true
   - Verify: original entity_id preserved
   - Verify: location zeroed or preserved (document which)

6. Add test "GeoEvent: should_copy_forward for tombstones":
   - Tombstone at non-final level: should copy forward
   - Tombstone at final level with no older versions: can be dropped
   - This is the compaction behavior for GDPR compliance
  </action>
  <verify>
```bash
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "delete"
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "GDPR"
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "tombstone"
```
All delete and GDPR compliance tests pass.
  </verify>
  <done>
- Delete removes entity from RAM index
- Delete creates proper tombstone
- Deleted entity not retrievable via any query type
- Batch delete works correctly
- Tombstone lifecycle verified (creation, propagation, compaction)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add TTL and query verification tests</name>
  <files>
    src/geo_state_machine.zig
  </files>
  <action>
Add tests for TTL expiration and query operations:

1. Add test "entity ops: TTL expiration":
   - Insert entity J with TTL = 60 seconds
   - Query at time T: entity J found
   - Query at time T + 61 seconds: entity J NOT found (expired)
   - Per CONTEXT.md: "TTL expiration removes expired entities automatically"

2. Add test "entity ops: TTL=0 never expires":
   - Insert entity K with TTL = 0
   - Query at time T + 1000000: entity K still found
   - TTL=0 means "never expires"

3. Add test "entity ops: UUID query returns correct entity":
   - Insert entities L1, L2, L3 with different UUIDs
   - Query UUID of L2
   - Verify only L2 returned
   - Verify all L2 fields correct

4. Add test "entity ops: batch UUID query":
   - Insert entities M1, M2, M3, M4, M5
   - Batch query [M1, M3, M5]
   - Verify exactly M1, M3, M5 returned
   - Verify order matches request order (or document actual order)

5. Add test "entity ops: latest query returns most recent":
   - Insert entity N at time T1, location L1
   - Upsert entity N at time T2, location L2
   - Query "latest" for entity N
   - Verify returns location L2 (most recent)
   - Per requirement ENT-08: "Latest query returns most recent position"

6. Add test "entity ops: TTL metrics":
   - Enable TTL metrics
   - Insert entity with TTL, let expire
   - Verify metrics incremented:
     - ttl_expirations counter
   - Per requirement ENT-10: "TTL cleanup metrics exposed"

Document: Requirements traceability for ENT-01 through ENT-10.
  </action>
  <verify>
```bash
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "TTL"
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "UUID query"
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "latest"
```
All TTL and query tests pass.
  </verify>
  <done>
- TTL expiration works correctly (entity gone after TTL)
- TTL=0 means never expires
- UUID query returns correct single entity
- Batch UUID query returns all requested entities
- Latest query returns most recent position
- TTL metrics exposed for GDPR compliance auditing
- Requirements ENT-01 through ENT-10 traced
  </done>
</task>

</tasks>

<verification>
All entity operation requirements verified:

```bash
# Run all entity operation tests
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "entity ops"
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "GeoEvent"
cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "tombstone"

# Verify requirements tracing
grep -n "ENT-0" src/geo_state_machine.zig src/geo_event.zig
```
</verification>

<success_criteria>
- Insert stores all GeoEvent fields correctly
- Insert on existing entity returns error (not silent overwrite)
- Upsert creates or updates with proper tombstone handling
- LWW semantics verified (newer timestamp wins)
- Delete removes from RAM index and creates tombstone
- Deleted entity not retrievable via any query (GDPR compliance)
- UUID query returns correct entity
- Batch UUID query returns all requested entities
- Latest query returns most recent position per entity
- TTL expiration works correctly
- TTL=0 means never expires
- TTL metrics exposed for compliance auditing
- Requirements ENT-01 through ENT-10 addressed
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-geospatial/03-04-SUMMARY.md`
</output>
