---
phase: 17-storage-validation-adaptive
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/benchmark-compaction.py
autonomous: true

must_haves:
  truths:
    - "Compaction benchmark can require actual ArcherDB runs and refuses estimation when requested."
    - "Tiered compaction results include throughput improvement as a pass/fail signal."
    - "JSON output records run mode, improvements, and per-strategy metrics for audit."
  artifacts:
    - path: "scripts/benchmark-compaction.py"
      provides: "Actual-mode enforcement and throughput pass/fail reporting"
  key_links:
    - from: "scripts/benchmark-compaction.py"
      to: "check_archerdb_available"
      via: "--require-archerdb flag"
      pattern: "require_archerdb"
    - from: "scripts/benchmark-compaction.py"
      to: "print_comparison"
      via: "throughput improvement evaluation"
      pattern: "TARGET_THROUGHPUT_IMPROVEMENT"
---

<objective>
Harden the compaction benchmark so tiered vs leveled comparisons are real and enforce throughput gains.

Purpose: Validate tiered compaction performance with actual runs instead of estimation.
Output: Compaction benchmark that enforces actual mode and reports throughput improvement pass/fail.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-storage-validation-adaptive/17-RESEARCH.md

@scripts/benchmark-compaction.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add require-archerdb enforcement for compaction runs</name>
  <files>scripts/benchmark-compaction.py</files>
  <action>
    - Add a --require-archerdb flag; when set and archerdb is missing, exit non-zero with a clear
      message instead of falling back to estimation.
    - Ensure mode selection honors require-archerdb even when --dry-run is omitted.
  </action>
  <verify>python3 scripts/benchmark-compaction.py --dry-run --duration 5 --rate 1000 --output /tmp/compaction.json</verify>
  <done>Benchmark can be forced into actual mode and exits when archerdb is unavailable.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce throughput improvement in comparison summary</name>
  <files>scripts/benchmark-compaction.py</files>
  <action>
    - Treat throughput improvement as a required signal (>= TARGET_THROUGHPUT_IMPROVEMENT) when
      computing the overall pass/fail in print_comparison().
    - Record pass/fail for throughput improvement in the JSON summary so validation is explicit.
    - Preserve existing write amplification checks and include both in the final passed value.
  </action>
  <verify>python3 scripts/benchmark-compaction.py --dry-run --duration 5 --rate 1000 --output /tmp/compaction.json</verify>
  <done>Benchmark summary explicitly flags throughput improvement and fails when it is not met.</done>
</task>

</tasks>

<verification>
- ./zig/zig build
- python3 scripts/benchmark-compaction.py --duration 60 --rate 10000 --output compaction-results.json --require-archerdb
</verification>

<success_criteria>
- Compaction benchmark runs in actual mode when required and reports if archerdb is missing.
- Tiered throughput improvement is evaluated alongside write amplification and recorded in output.
- Output JSON includes run mode, per-strategy metrics, and improvement deltas.
</success_criteria>

<output>
After completion, create `.planning/phases/17-storage-validation-adaptive/17-03-SUMMARY.md`.
</output>
