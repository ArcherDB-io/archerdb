---
phase: 17-storage-validation-adaptive
plan: 04
type: execute
wave: 2
depends_on: ["03"]
files_modified:
  - scripts/benchmark-compaction.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Compaction benchmark refuses estimation when --require-archerdb is set alongside --dry-run."
    - "Benchmark output records whether dry-run was requested and whether actual mode was enforced."
  artifacts:
    - path: "scripts/benchmark-compaction.py"
      provides: "Flag conflict enforcement and run-mode metadata"
      contains: "--require-archerdb"
  key_links:
    - from: "scripts/benchmark-compaction.py"
      to: "main flag validation"
      via: "args.require_archerdb + args.dry_run conflict guard"
      pattern: "require_archerdb.*dry_run"
    - from: "scripts/benchmark-compaction.py"
      to: "output_data"
      via: "run-mode metadata"
      pattern: "dry_run_requested|require_archerdb"
---

<objective>
Enforce require-archerdb actual-mode execution for the compaction benchmark.

Purpose: Close verification gap where --require-archerdb can still run in estimation mode.
Output: Compaction benchmark guardrails and audit metadata in JSON output.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/benchmark-compaction.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Block conflicting require-archerdb + dry-run flags</name>
  <files>scripts/benchmark-compaction.py</files>
  <action>
    Add explicit CLI validation in main(): if args.require_archerdb and args.dry_run are both set,
    print a clear error explaining they are incompatible and exit with a non-zero status (2).
    When require-archerdb is set and the binary exists, ensure args.dry_run is forced false so
    estimation cannot run.
  </action>
  <verify>python3 scripts/benchmark-compaction.py --require-archerdb --dry-run --output /tmp/compaction-results.json</verify>
  <done>Conflicting flags exit non-zero with a clear error and no estimation run occurs.</done>
</task>

<task type="auto">
  <name>Task 2: Record run-mode enforcement metadata in JSON output</name>
  <files>scripts/benchmark-compaction.py</files>
  <action>
    Extend output_data to include explicit metadata fields such as require_archerdb (bool) and
    dry_run_requested (bool) based on CLI args before enforcement. Keep the existing mode field
    derived from the enforced dry_run value. This provides auditability that actual mode was
    enforced when require-archerdb is used.
  </action>
  <verify>python3 scripts/benchmark-compaction.py --require-archerdb --output /tmp/compaction-results.json</verify>
  <done>JSON output includes require_archerdb and dry_run_requested fields and mode=actual when required.</done>
</task>

</tasks>

<verification>
- Running with --require-archerdb and --dry-run exits non-zero with an incompatibility error.
- Running with --require-archerdb alone produces JSON with mode=actual and audit fields.
</verification>

<success_criteria>
- Compaction benchmark can no longer run estimation when require-archerdb is set.
- Output JSON explicitly records run-mode enforcement metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/17-storage-validation-adaptive/17-04-SUMMARY.md`
</output>
