---
phase: 17-storage-validation-adaptive
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/benchmark-compression.py
autonomous: true

must_haves:
  truths:
    - "Compression benchmarks report reduction using raw logical bytes versus actual ArcherDB data file size."
    - "Benchmark runs can require a real archerdb binary and fail fast when missing."
    - "JSON output records baseline method and run mode for auditability."
  artifacts:
    - path: "scripts/benchmark-compression.py"
      provides: "Raw baseline calculation and require-archerdb gating"
  key_links:
    - from: "scripts/benchmark-compression.py"
      to: "check_archerdb_available"
      via: "--require-archerdb flag"
      pattern: "require_archerdb"
    - from: "scripts/benchmark-compression.py"
      to: "run_archerdb_benchmark"
      via: "compressed datafile measurement"
      pattern: "benchmark"
---

<objective>
Update the compression benchmark to use a real uncompressed baseline and enforce actual runs.

Purpose: Provide credible compression ratio validation that matches storage claims.
Output: Compression benchmark that compares logical bytes to actual data file size and records run mode.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-storage-validation-adaptive/17-RESEARCH.md

@scripts/benchmark-compression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add raw baseline and require-archerdb gating</name>
  <files>scripts/benchmark-compression.py</files>
  <action>
    - Add a CLI flag (--require-archerdb) that exits non-zero if archerdb is unavailable.
    - Replace the second "uncompressed" ArcherDB run with a logical baseline derived from
      events_to_bytes(), so the benchmark does not rely on a compression toggle.
    - When archerdb is available, run the benchmark once to measure compressed datafile size and
      compute reduction against the logical baseline.
  </action>
  <verify>python3 scripts/benchmark-compression.py --dry-run --output /tmp/compression.json</verify>
  <done>Benchmark can enforce real runs and uses a logical baseline for uncompressed bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend summary and JSON metadata for baseline mode</name>
  <files>scripts/benchmark-compression.py</files>
  <action>
    - Update printed summaries to label logical vs physical sizes and show baseline mode.
    - Extend JSON output with baseline metadata (e.g., baseline="logical-bytes"), archerdb path,
      and a mode value that distinguishes actual vs estimation.
    - Ensure fallback-to-estimation paths still set mode and baseline fields consistently.
  </action>
  <verify>python3 scripts/benchmark-compression.py --dry-run --output /tmp/compression.json</verify>
  <done>Benchmark output clearly identifies logical baseline, physical size, and run mode.</done>
</task>

</tasks>

<verification>
- ./zig/zig build
- python3 scripts/benchmark-compression.py --output compression-results.json --require-archerdb
</verification>

<success_criteria>
- Compression benchmark uses logical bytes as baseline and a real datafile size for compression ratio.
- --require-archerdb prevents estimation mode from silently passing as validation.
- Output JSON captures baseline mode, archerdb path, and pass/fail summary.
</success_criteria>

<output>
After completion, create `.planning/phases/17-storage-validation-adaptive/17-02-SUMMARY.md`.
</output>
