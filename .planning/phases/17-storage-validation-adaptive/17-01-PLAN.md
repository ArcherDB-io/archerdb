---
phase: 17-storage-validation-adaptive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lsm/manifest.zig
  - src/lsm/forest.zig
  - src/lsm/compaction.zig
  - src/constants.zig
autonomous: true

must_haves:
  truths:
    - "Adaptive compaction updates the effective L0 trigger and compaction thread limit at runtime when workload shifts."
    - "Operator overrides for L0 trigger or compaction threads keep compaction behavior fixed when set."
    - "Level-0 compaction eligibility uses the adaptive L0 trigger threshold instead of the static growth factor."
  artifacts:
    - path: "src/lsm/manifest.zig"
      provides: "Runtime L0 trigger override used by compaction_table"
    - path: "src/lsm/compaction.zig"
      provides: "ResourcePool CPU limiter for adaptive compaction threads"
    - path: "src/lsm/forest.zig"
      provides: "Adaptive recommendations applied to manifests and compaction pool"
  key_links:
    - from: "src/lsm/forest.zig"
      to: "set_l0_trigger_override"
      via: "adaptive_apply_recommendations"
      pattern: "adaptive_get_l0_trigger"
    - from: "src/lsm/forest.zig"
      to: "compaction_schedule.pool"
      via: "adaptive_get_compaction_threads"
      pattern: "cpu_limit"
    - from: "src/lsm/manifest.zig"
      to: "compaction_table"
      via: "level 0 table_count_visible_max override"
      pattern: "level_a == 0"
---

<objective>
Wire adaptive compaction recommendations into the L0 trigger and compaction thread limits at runtime.

Purpose: Ensure adaptive compaction does more than log recommendations by changing real compaction behavior.
Output: Manifest L0 thresholds and compaction resource limits that respond to adaptive state and overrides.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-storage-validation-adaptive/17-RESEARCH.md

@src/lsm/forest.zig
@src/lsm/manifest.zig
@src/lsm/compaction.zig
@src/lsm/compaction_adaptive.zig
@src/constants.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply adaptive L0 trigger to compaction selection</name>
  <files>src/lsm/manifest.zig, src/lsm/forest.zig</files>
  <action>
    - Add a runtime L0 trigger override on Manifest (default to constants.lsm_growth_factor).
    - Update compaction_table() to use the override when level_a == 0 while leaving other levels
      on table_count_max_for_level(). Keep existing assertions for table count bounds.
    - Add a Forest helper to update all tree manifests with forest.adaptive_get_l0_trigger(),
      call it during Forest.init(), and after adaptive_apply_recommendations() changes L0 trigger.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "adaptive"</verify>
  <done>Level 0 compaction threshold updates when adaptive L0 trigger changes.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce adaptive compaction thread limits</name>
  <files>src/lsm/compaction.zig, src/lsm/forest.zig, src/constants.zig</files>
  <action>
    - Define a max CPU slot count for compaction in constants (e.g., lsm_compaction_thread_slots_max).
    - Expand ResourcePool.cpus to the max slot count, add cpu_limit state, and add cpu_acquire()/
      cpu_release() helpers that respect cpu_limit before delegating to the underlying IOPSType.
    - Replace direct pool.cpus.acquire/release usage in compaction_dispatch with the new helpers.
    - Set pool.cpu_limit from forest.adaptive_get_compaction_threads() during Forest.init(), and
      update it whenever adaptive_apply_recommendations() changes compaction_threads.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "compaction"</verify>
  <done>Compaction CPU usage scales with adaptive thread recommendations and respects max slots.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize adaptive config and overrides from process constants</name>
  <files>src/constants.zig, src/lsm/forest.zig</files>
  <action>
    - Expose adaptive compaction settings from config.process via constants (enabled flag,
      write-change threshold, space amp threshold, override_l0_trigger, override_compaction_threads).
    - In Forest.init/reset, set forest.adaptive_config and override fields from constants,
      initialize adaptive_state current values to match the static defaults, and validate
      adaptive_config before first sampling.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "AdaptiveConfig"</verify>
  <done>Adaptive compaction respects configured enable/override values and stable defaults.</done>
</task>

</tasks>

<verification>
- ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "adaptive"
- ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "compaction"
</verification>

<success_criteria>
- Adaptive L0 trigger and compaction thread limits are applied to runtime compaction behavior.
- Operator overrides from config prevent adaptive changes when set.
- L0 compaction eligibility uses the adaptive threshold, not the static growth factor.
</success_criteria>

<output>
After completion, create `.planning/phases/17-storage-validation-adaptive/17-01-SUMMARY.md`.
</output>
