---
phase: 01-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/archerdb/metrics_server.zig
  - src/archerdb/main.zig
  - src/vsr/replica.zig
  - scripts/test-readiness-persistence.sh
autonomous: true

must_haves:
  truths:
    - "Server /health/ready returns 200 within 30 seconds of startup"
    - "Data written to server persists after restart"
    - "Server starts in production config (not --development) without errors"
  artifacts:
    - path: "scripts/test-readiness-persistence.sh"
      provides: "Combined reproduction and validation script"
      contains: "curl.*health/ready"
  key_links:
    - from: "src/archerdb/main.zig"
      to: "src/archerdb/metrics_server.zig"
      via: "markInitialized() call"
      pattern: "markInitialized"
    - from: "src/archerdb/metrics_server.zig"
      to: "replica.status"
      via: "server_initialized and isReady()"
      pattern: "server_initialized"
---

<objective>
Fix readiness probe (returns 503) and data persistence bugs that block production deployment.

Purpose: These two bugs block all other testing - if the server won't report ready and data doesn't persist, we can't validate concurrency or TTL cleanup. Fixing these first unblocks the rest of Phase 1.

Output:
- Working /health/ready endpoint returning 200 within 30 seconds
- Data persistence verified in production config (without --development flag)
- Combined test script that validates both bugs are fixed
- Comprehensive logging added for future debugging
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-bug-fixes/01-CONTEXT.md
@.planning/phases/01-critical-bug-fixes/01-RESEARCH.md

# Key source files (from RESEARCH.md)
@src/archerdb/metrics_server.zig
@src/archerdb/main.zig
@src/vsr/replica.zig
@src/archerdb/cli.zig
@src/config.zig

# Existing validation script for reference
@scripts/validate-readiness-fix.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate and fix readiness probe</name>
  <files>
    src/archerdb/metrics_server.zig
    src/archerdb/main.zig
    src/vsr/replica.zig
  </files>
  <action>
Debug why /health/ready returns 503:

1. **Add comprehensive logging** around replica status transitions:
   - Log every status change in replica.zig (recovering -> recovering_head -> normal)
   - Log when markInitialized() is called and server_initialized changes
   - Log the replica status when handleHealthReady is called
   - These logs stay in codebase permanently per CONTEXT.md

2. **Run existing validation script** to capture current failure:
   ```bash
   ./zig/zig build -j4 -Dconfig=lite && ./scripts/validate-readiness-fix.sh
   ```
   Capture the logs showing why it fails.

3. **Investigate root cause** from logs - likely one of:
   - Single-node replica not reaching .normal status
   - markInitialized() never called
   - Timing issue with server_initialized check

4. **Fix the issue** based on findings. Likely fixes:
   - For single-node cluster: ensure replica enters .normal immediately (no consensus needed)
   - Adjust initialization timing in main.zig
   - Ensure markInitialized() is called when replica becomes ready

5. **Run validation script again** - must now pass with 200 OK within 30 seconds.

Note: Use `-Dconfig=lite` for testing. Do NOT use --development flag when testing production behavior.
  </action>
  <verify>
1. `./scripts/validate-readiness-fix.sh` passes
2. Logs show "server marked as initialized" message
3. `curl http://127.0.0.1:$METRICS_PORT/health/ready` returns 200 with `{"status":"ok"}`
  </verify>
  <done>
/health/ready returns 200 OK within 2 seconds of server startup for single-node cluster.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix data persistence and create combined test</name>
  <files>
    src/archerdb/main.zig
    src/archerdb/cli.zig
    scripts/test-readiness-persistence.sh
  </files>
  <action>
Debug why data doesn't persist across restarts in production config:

1. **Create combined test script** `scripts/test-readiness-persistence.sh`:
   ```bash
   #!/bin/bash
   # Tests both readiness probe and data persistence
   # Run without --development flag to test production behavior

   # Steps:
   # 1. Format database
   # 2. Start server (NO --development flag)
   # 3. Wait for /health/ready to return 200
   # 4. Insert test entities via client
   # 5. Verify entities queryable
   # 6. Graceful shutdown
   # 7. Restart server
   # 8. Wait for ready again
   # 9. Verify entities still present
   # 10. Cleanup
   ```

2. **Run the test to capture failure evidence**:
   - Start server WITHOUT --development flag
   - Insert entities
   - Restart and check if entities persist
   - Capture logs showing what happens during shutdown/startup

3. **Investigate persistence failure**:
   - Check if data file is created and has content
   - Check if checkpoint/WAL is flushed on shutdown
   - Check if Direct I/O is enabled without --development
   - Add logging around superblock/WAL persistence

4. **Fix the persistence issue** based on findings:
   - Ensure graceful shutdown flushes WAL
   - Ensure checkpoint is written before shutdown completes
   - Verify data file survives restart

5. **Run combined test** - must pass both readiness and persistence checks.

Build with: `./zig/zig build -j4 -Dconfig=lite`
  </action>
  <verify>
1. `./scripts/test-readiness-persistence.sh` passes
2. Data file created at expected location with content > 0 bytes
3. Entities queryable after server restart
4. Works in both lite and production configs
  </verify>
  <done>
Data written via client persists across server restart. Combined test script passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests and commit</name>
  <files>
    src/testing/fixtures.zig
  </files>
  <action>
Add regression tests for readiness and persistence bugs:

1. **Check existing tests** in src/testing/ for patterns to follow.

2. **Add readiness probe test** (if testable in unit test framework):
   - Test that server_initialized logic works correctly
   - May need integration test if full server required

3. **Add persistence test** (if testable):
   - Test that WAL/checkpoint survives simulated restart
   - Check existing testing/storage.zig for patterns

4. **Run full test suite** to verify no regressions:
   ```bash
   ./zig/zig build -j4 -Dconfig=lite test:unit
   ```

5. **Create atomic commit** for readiness + persistence fixes:
   ```
   fix(01-01): readiness probe and data persistence

   - Readiness probe now returns 200 within 2 seconds of startup
   - Data persists across server restarts in production config
   - Added comprehensive logging for future debugging
   - Added regression tests
   - Added combined test script

   Fixes: CRIT-01, CRIT-02
   ```

Note: If regression tests are complex to add in unit test framework, the shell script serves as the regression test. Document this decision.
  </action>
  <verify>
1. `./zig/zig build -j4 -Dconfig=lite test:unit` passes
2. `./scripts/test-readiness-persistence.sh` passes
3. Git commit created with proper message
  </verify>
  <done>
Regression tests added (either in Zig or via shell script). Atomic commit created.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Readiness probe works:**
   - `./scripts/validate-readiness-fix.sh` passes
   - Health endpoint returns 200 within 30 seconds (actually within 2 seconds)

2. **Persistence works:**
   - `./scripts/test-readiness-persistence.sh` passes
   - Data survives restart in production config

3. **No regressions:**
   - `./zig/zig build -j4 -Dconfig=lite test:unit` passes
   - Pre-commit hooks pass

4. **Documentation:**
   - Comprehensive logging added and documented
   - Test scripts documented with expected behavior
</verification>

<success_criteria>
1. `/health/ready` returns 200 within 30 seconds of startup (CRIT-01 fixed)
2. Data persists across server restarts without --development flag (CRIT-02 fixed)
3. Both bugs have regression tests or test scripts
4. Single atomic commit with all changes
5. All tests pass in both lite and production configs
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-bug-fixes/01-01-SUMMARY.md`
</output>
