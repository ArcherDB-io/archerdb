---
phase: 01-critical-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/config.zig
  - src/connection_pool.zig
  - scripts/test-concurrent-clients.sh
autonomous: true

must_haves:
  truths:
    - "Server handles 100 concurrent clients without failures"
    - "Server handles 200+ concurrent clients (stress test beyond minimum)"
    - "Connection pool scales appropriately with client count"
  artifacts:
    - path: "scripts/test-concurrent-clients.sh"
      provides: "Concurrency stress test script"
      contains: "concurrent.*100"
  key_links:
    - from: "src/config.zig"
      to: "connection_pool"
      via: "clients_max configuration"
      pattern: "clients_max"
    - from: "src/connection_pool.zig"
      to: "coordinator"
      via: "max_connections setting"
      pattern: "max_connections"
---

<objective>
Fix concurrent client handling (currently fails at 10 clients, needs to handle 100+).

Purpose: Multi-node testing and production workloads require handling many concurrent clients. The current lite config limits to 7 clients, causing failures when testing with 10+.

Output:
- Server handles 100+ concurrent clients
- Stress test finds new breaking point (not just meet minimum)
- Concurrency test script for ongoing validation
- Understanding of clients_max vs connection pool limits
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-bug-fixes/01-CONTEXT.md
@.planning/phases/01-critical-bug-fixes/01-RESEARCH.md

# Key source files (from RESEARCH.md)
@src/config.zig
@src/connection_pool.zig
@src/coordinator.zig
@src/constants.zig

# Prior plan summary (if generated)
# @.planning/phases/01-critical-bug-fixes/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate concurrency limits and create test</name>
  <files>
    src/config.zig
    src/connection_pool.zig
    src/coordinator.zig
    scripts/test-concurrent-clients.sh
  </files>
  <action>
Investigate why server fails at 10 concurrent clients:

1. **Examine configuration values** (from RESEARCH.md):
   - Lite config: `clients_max = 7` (config.zig:661-662)
   - Production config: `clients_max = 64` (config.zig:492-494)
   - Connection pool: `max_connections = 32` (connection_pool.zig:23)
   - Coordinator: `max_connections = 10000` (coordinator.zig)

2. **Add logging** to track client connections:
   - Log when client connects/disconnects
   - Log when connection limit is reached
   - Log pool utilization statistics

3. **Create test script** `scripts/test-concurrent-clients.sh`:
   ```bash
   #!/bin/bash
   # Tests concurrent client handling
   # Requirement: Handle 100+ concurrent clients

   # Steps:
   # 1. Start server
   # 2. Launch N concurrent clients (start with 10, then 50, 100, 200)
   # 3. Each client: insert entity, query it, delete it
   # 4. Measure success rate and latency
   # 5. Report max concurrent clients before failure
   ```

4. **Run test with current config** to reproduce failure at 10 clients.
   Use lite config first, then production config.

5. **Identify the bottleneck**:
   - Is it clients_max (7 in lite)?
   - Is it connection_pool max_connections (32)?
   - Is it something else?
  </action>
  <verify>
1. Test script runs and reports current failure point
2. Logs show which limit is reached
3. Clear understanding of clients_max vs connection pool interaction
  </verify>
  <done>
Root cause identified: which configuration limit causes failure at 10 clients.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix concurrency limits and stress test</name>
  <files>
    src/config.zig
    src/connection_pool.zig
    scripts/test-concurrent-clients.sh
  </files>
  <action>
Fix the concurrency limitation and stress test beyond requirements:

1. **Determine fix strategy** based on Task 1 findings:

   **Option A:** Increase lite config limits (if that's the bottleneck)
   - Change `clients_max` from 7 to higher value
   - Keep RAM constraints in mind per CLAUDE.md

   **Option B:** Use production config for concurrency testing
   - Document that lite config is unsuitable for concurrency tests
   - Ensure production config is used in CI for this test

   **Option C:** Fix connection pool limits
   - Align max_connections with clients_max
   - Ensure pool doesn't reject valid connections

2. **Implement the fix** - modify config.zig or connection_pool.zig as needed.

3. **Test incrementally** (per CONTEXT.md - stress test beyond 100):
   - Test with 10 clients: should pass
   - Test with 50 clients: should pass
   - Test with 100 clients: should pass (requirement)
   - Test with 200 clients: find new breaking point
   - Test with 500 clients: find actual limit

4. **Update test script** with final results:
   - Document actual max concurrent clients supported
   - Document which config (lite vs production) to use
   - Add clear pass/fail criteria

5. **Verify in both configs** per CONTEXT.md decision:
   ```bash
   # Lite config (may have lower limit - document it)
   ./zig/zig build -j4 -Dconfig=lite && ./scripts/test-concurrent-clients.sh

   # Production config (should handle 100+)
   ./zig/zig build -j4 -Dconfig=production && ./scripts/test-concurrent-clients.sh
   ```
  </action>
  <verify>
1. `./scripts/test-concurrent-clients.sh` passes with 100 clients
2. Stress test shows handling of 200+ clients (or documented limit)
3. Both lite and production configs tested
4. New breaking point identified and documented
  </verify>
  <done>
Server handles 100+ concurrent clients. Stress test reveals actual maximum capacity.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression test and commit</name>
  <files>
    src/testing/fixtures.zig
  </files>
  <action>
Add regression test and create atomic commit:

1. **Check if unit test is feasible**:
   - Look at existing concurrency tests in src/testing/
   - If integration test exists, add case for 100 clients

2. **Add regression test** (or document shell script as regression test):
   - Test that clients_max >= 64 in production config
   - Test that connection pool doesn't reject below clients_max

3. **Run full test suite**:
   ```bash
   ./zig/zig build -j4 -Dconfig=lite test:unit
   ```

4. **Create atomic commit**:
   ```
   fix(01-02): concurrent client handling

   - Server now handles 100+ concurrent clients
   - Identified limit: [actual limit found in stress test]
   - Added concurrency test script
   - Added logging for connection tracking
   - [Document config choice: lite vs production]

   Fixes: CRIT-03
   ```

5. **Run validation checklist item** for concurrency:
   - Per CONTEXT.md: "Re-run entire DATABASE_VALIDATION_CHECKLIST.md after each fix"
   - At minimum, run the concurrent_clients.py check if it exists
  </action>
  <verify>
1. `./zig/zig build -j4 -Dconfig=lite test:unit` passes
2. `./scripts/test-concurrent-clients.sh` passes with 100 clients
3. Git commit created with proper message
4. Validation checklist item passes
  </verify>
  <done>
Concurrent client handling fixed. Server handles 100+ clients. Atomic commit created.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **100 concurrent clients works:**
   - `./scripts/test-concurrent-clients.sh 100` passes

2. **Stress test results documented:**
   - Actual maximum concurrent clients known
   - Breaking point > 100 identified

3. **No regressions:**
   - `./zig/zig build -j4 -Dconfig=lite test:unit` passes
   - `./scripts/test-readiness-persistence.sh` still passes

4. **Both configs tested:**
   - Lite config behavior documented
   - Production config handles 100+
</verification>

<success_criteria>
1. Server handles 100 concurrent clients without failures (CRIT-03 fixed)
2. Stress test shows handling beyond 100 (200+ or documented limit)
3. Test script created and passes
4. Atomic commit created
5. Prior fixes (readiness, persistence) still work
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-bug-fixes/01-02-SUMMARY.md`
</output>
