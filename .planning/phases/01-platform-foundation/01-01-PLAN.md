---
phase: 01-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/io/windows.zig
  - src/io.zig
  - src/stdx/windows.zig
  - src/stdx/stdx.zig
  - src/stdx/mlock.zig
  - src/shell.zig
  - src/unit_tests.zig
  - src/multiversion.zig
  - src/build_multiversion.zig
  - src/vortex.zig
  - src/archerdb/main.zig
  - src/testing/tmp_archerdb.zig
  - src/testing/vortex/supervisor.zig
  - src/testing/vortex/logged_process.zig
  - src/scripts/cfo.zig
  - src/repl/terminal.zig
  - build.zig
autonomous: true

must_haves:
  truths:
    - "Build succeeds on Linux with no Windows references"
    - "Build succeeds on macOS with no Windows references"
    - "No file contains Windows conditionals or imports"
    - "Compiler errors if targeting Windows platform"
  artifacts:
    - path: "src/io.zig"
      provides: "Platform hub selecting Linux or Darwin only"
      contains: "@compileError"
    - path: "src/io/windows.zig"
      provides: "DELETED - file should not exist"
    - path: "src/stdx/windows.zig"
      provides: "DELETED - file should not exist"
  key_links:
    - from: "src/io.zig"
      to: "builtin.target.os.tag"
      via: "comptime switch"
      pattern: "else => @compileError"
---

<objective>
Remove all Windows platform support code from the codebase.

Purpose: Per project decision, Windows support is dropped to reduce maintenance burden. This plan systematically removes all Windows-specific code, conditionals, and build targets, leaving a cleaner Linux/Darwin-only codebase.

Output: Codebase compiles only for Linux and macOS, with Windows references eliminated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Windows I/O implementation and stdx support</name>
  <files>
    src/io/windows.zig
    src/stdx/windows.zig
    src/stdx/stdx.zig
    src/stdx/mlock.zig
  </files>
  <action>
Delete the Windows-specific files:
- DELETE `src/io/windows.zig` (1641 lines)
- DELETE `src/stdx/windows.zig` (Windows API declarations)

Update files that import or reference Windows:
- `src/stdx/stdx.zig`: Remove any `@import("windows.zig")` or Windows-related exports
- `src/stdx/mlock.zig`: Remove the `.windows` switch arm (lines 28, 56-87), remove `os.windows` references, update to use only POSIX mlock

For mlock.zig specifically:
- Remove `memory_lock_allocated_windows` function entirely
- Remove the `.windows =>` case from the switch at line 28
- Keep only the `.linux, .macos, ...` POSIX implementation
  </action>
  <verify>
Files deleted: `ls src/io/windows.zig src/stdx/windows.zig` should return "No such file"
Build check: `./zig/zig build --help` succeeds (validates syntax)
  </verify>
  <done>
windows.zig files deleted, stdx updated to remove all Windows references
  </done>
</task>

<task type="auto">
  <name>Task 2: Update io.zig hub and source files with Windows conditionals</name>
  <files>
    src/io.zig
    src/shell.zig
    src/unit_tests.zig
    src/multiversion.zig
    src/build_multiversion.zig
    src/vortex.zig
    src/archerdb/main.zig
    src/testing/tmp_archerdb.zig
    src/testing/vortex/supervisor.zig
    src/testing/vortex/logged_process.zig
    src/scripts/cfo.zig
    src/repl/terminal.zig
  </files>
  <action>
Update `src/io.zig`:
- Remove line 9: `const IO_Windows = @import("io/windows.zig").IO;`
- Change the switch to:
```zig
pub const IO = switch (builtin.target.os.tag) {
    .linux => IO_Linux,
    .macos, .tvos, .watchos, .ios => IO_Darwin,
    else => @compileError("IO is not supported for this platform. ArcherDB requires Linux or macOS."),
};
```
- In `buffer_limit` function (lines 30-35), remove the generic `else` case or make it explicit for Darwin only

For each source file with Windows conditionals:
- `src/shell.zig` (lines 254, 1039): Remove `if (builtin.os.tag != .windows)` guards - make POSIX code unconditional
- `src/unit_tests.zig` (lines 218, 353): Remove Windows-specific test code paths
- `src/multiversion.zig`: Remove any Windows architecture handling
- `src/build_multiversion.zig`: Remove Windows targets from architecture maps (lines 28-29, 84, 151, 446, 504, 564, 682, 764)
- `src/vortex.zig`: Remove Windows conditionals if present
- `src/archerdb/main.zig`: Remove Windows-specific startup code
- `src/testing/tmp_archerdb.zig`: Remove Windows path handling
- `src/testing/vortex/*`: Remove Windows conditionals
- `src/scripts/cfo.zig`: Remove Windows conditionals
- `src/repl/terminal.zig`: Remove Windows terminal handling

Pattern to find: `builtin.os.tag == .windows`, `builtin.os.tag != .windows`, `@import("windows")`
  </action>
  <verify>
Grep check: `grep -r "\.windows" src/ --include="*.zig" | grep -v "^src/clients"` returns empty (no Windows refs in core)
Build syntax: `./zig/zig build --help` succeeds
  </verify>
  <done>
All source files updated, Windows conditionals removed, io.zig hub shows compile error for unsupported platforms
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean build.zig and validate build</name>
  <files>
    build.zig
  </files>
  <action>
Update `build.zig` to remove all Windows targets and references:

1. Remove Windows from target list (line 23): Delete `"x86_64-windows"` from supported targets
2. Remove Windows objcopy fetch (lines 801-808): Delete the `.windows =>` case in objcopy function
3. Remove `set_windows_dll` function (lines 1752-1769) and its extern declaration (line 1769)
4. Remove Windows-specific Java handling (line 1093)
5. Remove all conditionals checking `os.tag == .windows` throughout:
   - Line 659: executable filename extension
   - Line 770: basename handling
   - Line 1049: Java lib path
   - Line 1061: optimize mode
   - Line 1270: Windows-specific options
   - Line 1311: target mapping
   - Line 1454, 1496, 1554, 1618, 1680, 1733: Various Windows conditionals
   - Line 2038: OS name mapping
   - Line 2058: executable extension

For each conditional, either:
- Remove entirely if Windows-only code
- Keep only the Linux/macOS path if multi-platform
- Update error messages to not mention Windows

Ensure the target list only includes:
- x86_64-linux
- aarch64-linux
- x86_64-macos
- aarch64-macos
  </action>
  <verify>
Full build: `./zig/zig build` completes without error
Unit tests subset: `./zig/zig build test:unit -- --test-filter "parse_args"` passes
Grep: `grep -i windows build.zig` returns empty
  </verify>
  <done>
build.zig cleaned of Windows, build succeeds on current platform, only Linux/macOS targets remain
  </done>
</task>

</tasks>

<verification>
1. Files deleted: `ls src/io/windows.zig src/stdx/windows.zig 2>&1 | grep -q "No such file"`
2. No Windows in source: `grep -rE "\.windows|windows\." src/ --include="*.zig" | grep -v clients | wc -l` returns 0
3. No Windows in build: `grep -i windows build.zig | wc -l` returns 0
4. Build succeeds: `./zig/zig build` exits 0
5. Quick tests pass: `./zig/zig build test:unit -- --test-filter "parse_args"` exits 0
</verification>

<success_criteria>
- windows.zig files deleted from io/ and stdx/
- No `builtin.os.tag == .windows` conditionals remain in src/
- No Windows targets in build.zig
- io.zig hub emits @compileError for non-Linux/Darwin platforms
- Build completes successfully
- Representative tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation/01-01-SUMMARY.md`
</output>
