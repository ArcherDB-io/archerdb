---
phase: 11-measurement-profiling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/flamegraph.sh
  - scripts/profile.sh
  - docs/profiling.md
autonomous: true

must_haves:
  truths:
    - "Developer can generate CPU flame graphs from any ArcherDB workload"
    - "Flame graph shows complete call stacks (no [unknown] frames)"
    - "Helper script works with both attached processes and new commands"
  artifacts:
    - path: "scripts/flamegraph.sh"
      provides: "Flame graph generation wrapper"
      contains: "perf record"
    - path: "scripts/profile.sh"
      provides: "General CPU profiling helper"
      contains: "perf stat"
    - path: "docs/profiling.md"
      provides: "Developer documentation for profiling workflow"
      contains: "flame graph"
  key_links:
    - from: "scripts/flamegraph.sh"
      to: "FlameGraph scripts"
      via: "stackcollapse-perf.pl | flamegraph.pl"
      pattern: "flamegraph\\.pl"
---

<objective>
Implement CPU profiling infrastructure with Linux perf integration.

Purpose: Enable developers to identify CPU bottlenecks using industry-standard flame graphs. Frame pointers are already preserved in build.zig, so perf integration is straightforward.

Output: Helper scripts for flame graph generation and CPU profiling, plus developer documentation.
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-measurement-profiling/11-RESEARCH.md

Reference existing scripts:
@scripts/run-perf-benchmarks.sh (existing benchmark runner pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create flame graph helper script</name>
  <files>scripts/flamegraph.sh</files>
  <action>
  Create scripts/flamegraph.sh that wraps Linux perf and FlameGraph scripts:

  1. Accept command line options:
     - `-d, --duration <sec>` - Sampling duration (default: 30)
     - `-f, --frequency <hz>` - Sampling frequency (default: 99)
     - `-a, --all` - Include kernel stacks
     - `-p, --pid <pid>` - Attach to existing process
     - `--output <file.svg>` - Output file (required)
     - `-- <command>` - Command to profile

  2. Auto-detect FlameGraph scripts location:
     - Check FLAMEGRAPH_DIR env var first
     - Fall back to tools/FlameGraph or /usr/share/FlameGraph
     - Error with clear install instructions if not found

  3. Run perf record with proper settings:
     - Use `--call-graph dwarf` for complete stack traces
     - Use provided frequency (default 99Hz to avoid lockstep sampling)
     - Handle both PID attachment and command execution

  4. Generate flame graph:
     - Run `perf script | stackcollapse-perf.pl | flamegraph.pl`
     - Set title to "ArcherDB CPU Profile - {workload}"
     - Clean up perf.data file on completion

  5. Print success message with output path

  Use existing scripts/run-perf-benchmarks.sh as style reference for arg parsing, color output, and error handling.
  </action>
  <verify>
  ```bash
  # Verify script is executable and has correct syntax
  bash -n scripts/flamegraph.sh
  chmod +x scripts/flamegraph.sh
  scripts/flamegraph.sh --help
  ```
  </verify>
  <done>Script exists, is executable, shows help with all documented options</done>
</task>

<task type="auto">
  <name>Task 2: Create CPU profiling helper script</name>
  <files>scripts/profile.sh</files>
  <action>
  Create scripts/profile.sh for hardware counter profiling with perf stat:

  1. Accept command line options:
     - `-c, --counters <list>` - Hardware counters (default: cycles,instructions,cache-misses,branch-misses)
     - `-r, --repeat <n>` - Number of runs for statistics (default: 5)
     - `-d, --detailed` - Show detailed per-core breakdown
     - `--json` - Output in JSON format for CI
     - `-- <command>` - Command to profile

  2. Run perf stat with:
     - Selected hardware counters
     - Multiple runs for statistical reliability
     - Output both human-readable and optional JSON

  3. For JSON output, structure as:
     ```json
     {
       "counters": {
         "cycles": { "mean": N, "stddev": N },
         "instructions": { "mean": N, "stddev": N },
         ...
       },
       "derived": {
         "ipc": N,
         "cache_miss_rate": N,
         "branch_miss_rate": N
       }
     }
     ```

  4. Print summary with derived metrics (IPC, cache miss rate, branch miss rate)
  </action>
  <verify>
  ```bash
  bash -n scripts/profile.sh
  chmod +x scripts/profile.sh
  scripts/profile.sh --help
  ```
  </verify>
  <done>Script exists, is executable, shows help with all documented options</done>
</task>

<task type="auto">
  <name>Task 3: Create profiling documentation</name>
  <files>docs/profiling.md</files>
  <action>
  Create docs/profiling.md with comprehensive profiling guide:

  1. Prerequisites section:
     - Linux kernel >= 5.6 with perf support
     - FlameGraph scripts installation (git clone command)
     - perf installation (apt install linux-perf)
     - Note about frame pointers already preserved in ArcherDB builds

  2. Quick Start section:
     - Generate flame graph: `./scripts/flamegraph.sh --output profile.svg -- ./zig-out/bin/archerdb benchmark`
     - View in browser: `xdg-open profile.svg`

  3. Flame Graph Guide:
     - How to read flame graphs (width = time spent, y-axis = stack depth)
     - Common patterns to look for (hot functions, deep stacks, unexpected callers)
     - Example interpretation with ArcherDB-specific tips

  4. Hardware Counter Profiling:
     - Using scripts/profile.sh for IPC, cache misses, branch misses
     - Interpreting results (what's good IPC for ArcherDB workloads)

  5. Profiling Workflows:
     - Profiling a specific benchmark
     - Profiling production workload (attach to PID)
     - A/B comparison workflow (reference to POOP in next plan)

  6. Troubleshooting:
     - "perf: command not found" - installation instructions
     - "[unknown]" frames in flame graph - should not happen (frame pointers preserved)
     - Permission denied - perf_event_paranoid settings
  </action>
  <verify>
  ```bash
  # Verify markdown syntax and file exists
  ls -la docs/profiling.md
  head -50 docs/profiling.md
  ```
  </verify>
  <done>Documentation file exists with Prerequisites, Quick Start, Flame Graph Guide, and Troubleshooting sections</done>
</task>

</tasks>

<verification>
Overall plan verification:
1. `scripts/flamegraph.sh --help` shows usage with all options
2. `scripts/profile.sh --help` shows usage with all options
3. `docs/profiling.md` exists and contains all required sections
4. Both scripts pass bash syntax check (`bash -n`)
</verification>

<success_criteria>
- PROF-01 requirement satisfied: CPU profiling with Linux perf integration
- Flame graph script ready for use
- Hardware counter profiling script ready for use
- Developer documentation complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-measurement-profiling/11-01-SUMMARY.md`
</output>
