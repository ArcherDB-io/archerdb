---
phase: 05-sharding-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/archerdb/cli.zig
  - src/archerdb/main.zig
  - Various files with TODO/FIXME markers
autonomous: true

must_haves:
  truths:
    - "Deprecated --aof flag is removed from CLI parser and documentation"
    - "All TODO comments in production code are resolved (not deferred)"
    - "No FIXME/XXX/HACK/BUG markers remain in production code"
    - "Code compiles and all existing tests pass after cleanup"
  artifacts:
    - path: "src/archerdb/cli.zig"
      provides: "--aof flag removed"
      not_contains: "--aof is deprecated"
    - path: "src/archerdb/main.zig"
      provides: "AOF warning removed"
      not_contains: "started with --aof"
  key_links:
    - from: "src/archerdb/cli.zig"
      to: "src/archerdb/main.zig"
      via: "CLI option parsing"
      pattern: "aof_file"
---

<objective>
Remove deprecated --aof flag and resolve all TODO/FIXME markers in production Zig code - no deferrals, fix everything now.

Purpose: Technical debt obscures real issues and slows development. Clean code is maintainable code. Per CONTEXT.md, all TODOs resolved in this phase.

Output: Clean codebase with no deprecated flags, no unresolved TODOs in production code, all tests passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sharding-cleanup/05-CONTEXT.md
@.planning/phases/05-sharding-cleanup/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated --aof flag</name>
  <files>src/archerdb/cli.zig, src/archerdb/main.zig</files>
  <action>
Remove the deprecated --aof flag completely:

1. In src/archerdb/cli.zig:
   - Remove the --aof option definition
   - Remove the mutual exclusivity check with --aof-file (line ~2108)
   - Remove the deprecated warning log (line ~2119)
   - Keep --aof-file as the only AOF option
   - Update any help text that mentions --aof

2. In src/archerdb/main.zig:
   - Remove the warning about "started with --aof" (line ~903)
   - Update any AOF-related logic that checks for the old flag

3. Search for any other references to the deprecated flag:
   - Documentation files
   - Test files
   - Config examples

The --aof-file flag remains as the supported option. Users must migrate to --aof-file=path syntax.

Per CONTEXT.md TODO resolution strategy: This is CLEAN-01, remove deprecated flag.
  </action>
  <verify>./zig/zig build && grep -rn "\-\-aof[^-]" src/ | grep -v "aof-file" | wc -l | grep -q "^0$" && echo "PASS: --aof removed"</verify>
  <done>--aof flag completely removed, only --aof-file remains, code compiles, no references to deprecated flag</done>
</task>

<task type="auto">
  <name>Task 2: Resolve TODO comments in core VSR and storage code</name>
  <files>
    src/vsr/*.zig
    src/storage.zig
    src/lsm/*.zig
    src/config.zig
  </files>
  <action>
Resolve all TODO comments in VSR, storage, and LSM code following CONTEXT.md strategy:

**Resolution approach per CONTEXT.md:**
- FIXMEs first (treat as bugs)
- Decision TODOs ("X or Y?") - pick the more performant option
- Error handling TODOs - critical path fails fast, background tasks handle gracefully
- All TODOs resolved here, no deferral to issues

**For each TODO, apply appropriate resolution:**

1. **Performance TODOs** - Implement the optimization if straightforward, or document why current approach is sufficient
2. **Cleanup TODOs** - Do the cleanup now
3. **Future feature TODOs** - Evaluate correctness/stability impact:
   - If needed for correctness: implement
   - If purely enhancement: document in code comment as "Enhancement:" instead of "TODO:"
4. **Duplicate TODOs** - Fix each instance individually

**Key files to process:**
- src/vsr/replica.zig (~16 TODOs)
- src/storage.zig (~9 TODOs)
- src/lsm/manifest_log.zig (~6 TODOs)
- src/vopr.zig (~6 TODOs)
- src/vsr/superblock.zig (~5 TODOs)
- src/vsr/message_header.zig (~5 TODOs)
- src/lsm/scan_builder.zig (~7 TODOs)

**DO NOT:**
- Change core algorithms without understanding full impact
- Remove safety checks
- Break existing tests
  </action>
  <verify>./zig/zig build && ./zig/zig build test:unit -- --test-filter "vsr" 2>&1 | head -20</verify>
  <done>All TODOs in VSR, storage, and LSM code resolved or converted to "Enhancement:" comments, code compiles, tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Resolve remaining TODOs and verify cleanup</name>
  <files>
    src/trace/event.zig
    src/message_pool.zig
    src/testing/*.zig
    src/clients/*.zig
    src/scripts/*.zig
    src/archerdb/*.zig
  </files>
  <action>
Resolve remaining TODO comments and perform final verification:

1. **Resolve TODOs in:**
   - src/trace/event.zig (~2 TODOs)
   - src/message_pool.zig (~1 TODO)
   - src/testing/ files (~various)
   - src/clients/ files (~various, per CONTEXT.md resolve SDK TODOs here not Phase 6)
   - src/scripts/ files (~various)
   - src/archerdb/ files (~remaining)

2. **Handle test-only TODOs:**
   - Testing infrastructure TODOs can be marked as "TestEnhancement:" if not critical
   - Fuzzer TODOs that improve coverage but don't affect correctness: implement or document

3. **Final verification scan:**
   ```bash
   grep -rn "// TODO" src/ --include="*.zig" | grep -v "_test.zig" | grep -v "testing/" | wc -l
   ```
   Target: 0 production TODOs remaining (test files may have some)

4. **FIXME/XXX/HACK/BUG scan:**
   - These should already be zero in production code
   - Verify none were introduced during cleanup

**Per CONTEXT.md:**
- Documentation TODOs can be marked "DocTODO:" and deferred to Phase 9
- All other TODOs resolved now
  </action>
  <verify>./zig/zig build && grep -rn "// TODO" src/ --include="*.zig" | grep -v "test" | grep -v "fuzz" | wc -l</verify>
  <done>All production TODOs resolved, no FIXME/XXX/HACK/BUG in production code, build passes, tests pass</done>
</task>

</tasks>

<verification>
Full cleanup verification:
```bash
# Build passes
./zig/zig build

# No deprecated --aof flag
grep -rn "\-\-aof[^-]" src/ | grep -v "aof-file" && echo "FAIL: --aof still exists" || echo "PASS"

# Count remaining TODOs (excluding tests and fuzzing)
echo "Remaining TODOs:"
grep -rn "// TODO" src/ --include="*.zig" | grep -v "test" | grep -v "fuzz" | wc -l

# No FIXMEs in production code
grep -rn "// FIXME" src/ --include="*.zig" | grep -v "test" | grep -v "tidy.zig" | wc -l

# Run tests
./zig/zig build test:unit 2>&1 | tail -5
```
</verification>

<success_criteria>
- [ ] --aof flag completely removed (CLEAN-01)
- [ ] Production TODOs reduced to zero or documented as deferred enhancements
- [ ] No FIXME/XXX/HACK/BUG markers in production code (CLEAN-03)
- [ ] Build passes: `./zig/zig build`
- [ ] Tests pass: `./zig/zig build test:unit`
</success_criteria>

<output>
After completion, create `.planning/phases/05-sharding-cleanup/05-02-SUMMARY.md`
</output>
