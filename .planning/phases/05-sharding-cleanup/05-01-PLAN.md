---
phase: 05-sharding-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sharding.zig
  - src/clients/go/pkg/types/geo_sharding.go
  - src/clients/python/src/archerdb/types.py
  - src/clients/java/src/main/java/com/archerdb/geo/ShardingStrategy.java
  - src/clients/node/src/geo.ts
autonomous: true

must_haves:
  truths:
    - "Jump hash produces identical results for same inputs across Zig and all SDKs"
    - "Distribution variance is within +/-5% for all shard counts 8-256"
    - "Resharding moves exactly 1/(N+1) entities when adding one shard"
    - "Entity routing is deterministic - same entity always routes to same shard"
  artifacts:
    - path: "src/sharding.zig"
      provides: "Golden vector tests for jump hash verification"
      contains: "test \"jumpHash golden vectors - cross-SDK\""
    - path: "src/sharding.zig"
      provides: "Distribution tolerance tests"
      contains: "test \"jumpHash distribution within 5%\""
  key_links:
    - from: "src/sharding.zig"
      to: "src/clients/*/sharding"
      via: "golden vector test values"
      pattern: "0xDEADBEEF.*expected"
---

<objective>
Verify sharding correctness: jump consistent hash produces identical results across Zig server and all SDK implementations, distribution is even within tolerance, and resharding behavior is optimal.

Purpose: Sharding is foundational for distributed operation - if clients compute different shards than the server, data becomes unreachable. Verification now prevents production incidents.

Output: Golden vector tests in sharding.zig, cross-SDK verification tests, distribution tolerance tests - all passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sharding-cleanup/05-CONTEXT.md
@.planning/phases/05-sharding-cleanup/05-RESEARCH.md
@src/sharding.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add golden vector tests for jump hash cross-SDK verification</name>
  <files>src/sharding.zig</files>
  <action>
Add comprehensive golden vector tests for jumpHash() to verify cross-SDK compatibility:

1. Compute and document canonical golden vectors:
   - Key 0 with buckets 1, 10, 100, 256
   - Key 0xDEADBEEF with buckets 8, 16, 32, 64, 128, 256
   - Key 0xCAFEBABE with buckets 8, 16, 32, 64, 128, 256
   - Key max u64 (0xFFFFFFFFFFFFFFFF) with buckets 8, 16, 256
   - Random keys: 0x123456789ABCDEF0, 0xFEDCBA9876543210

2. Add test "jumpHash golden vectors - cross-SDK compatibility" that:
   - Tests all golden vectors
   - Documents that these values MUST match SDK implementations
   - Includes comment with reference to Google's original algorithm

3. Add test "jumpHash determinism" that:
   - Verifies same key+buckets always produces same result
   - Runs 1000 iterations with same inputs
   - Confirms no state leakage between calls

4. Add test "computeShardKey determinism" that:
   - Verifies same entity_id always produces same shard_key
   - Tests with known entity_id values

Per CONTEXT.md: Verify existing implementation, do NOT modify the core algorithm.
  </action>
  <verify>./zig/zig build test:unit -- --test-filter "jumpHash golden" && ./zig/zig build test:unit -- --test-filter "jumpHash determinism" && ./zig/zig build test:unit -- --test-filter "computeShardKey determinism"</verify>
  <done>Golden vector tests pass, all documented vectors produce expected results, determinism verified over 1000+ iterations</done>
</task>

<task type="auto">
  <name>Task 2: Add distribution tolerance and resharding tests</name>
  <files>src/sharding.zig</files>
  <action>
Add distribution and resharding verification tests:

1. Add test "jumpHash distribution within 5%" that:
   - Tests with 8, 16, 32, 64, 128, 256 shards
   - Uses 100,000 random keys (PRNG with documented seed 12345)
   - Verifies each shard receives within +/-5% of expected (per CONTEXT.md strict tolerance)
   - Reports actual max variance for documentation

2. Add test "jumpHash resharding optimal movement" that:
   - Distributes 10,000 keys across N shards
   - Increases to N+1 shards
   - Verifies exactly ~1/(N+1) keys moved (within 1% tolerance for statistical variance)
   - Tests transitions: 8->9, 16->17, 100->101, 255->256

3. Add test "getShardForEntityWithStrategy consistency" that:
   - Verifies all strategies produce deterministic results
   - Tests modulo, virtual_ring, jump_hash, spatial strategies
   - Confirms same entity always routes to same shard

4. Add ShardStats.maxImbalance() test to verify calculation correctness

Per CONTEXT.md: +/-5% variance is the strict tolerance, each shard must be within 5% of ideal.
  </action>
  <verify>./zig/zig build test:unit -- --test-filter "distribution within 5%" && ./zig/zig build test:unit -- --test-filter "resharding optimal"</verify>
  <done>Distribution tests pass with all shard counts within 5% tolerance, resharding moves ~1/(N+1) keys as expected</done>
</task>

<task type="auto">
  <name>Task 3: Verify SDK jump hash implementations match golden vectors</name>
  <files>
    src/clients/go/pkg/types/geo_sharding.go
    src/clients/python/src/archerdb/types.py
    src/clients/java/src/main/java/com/archerdb/geo/ShardingStrategy.java
    src/clients/node/src/geo.ts
  </files>
  <action>
For each SDK, verify and add golden vector tests that match the Zig implementation:

1. **Go SDK** (src/clients/go/pkg/types/geo_sharding.go):
   - Find existing jumpHash implementation
   - Add TestJumpHashGoldenVectors test with same vectors as Zig
   - Verify results match Zig golden vectors exactly

2. **Python SDK** (src/clients/python/src/archerdb/types.py):
   - Find existing jump_hash implementation
   - Add test_jump_hash_golden_vectors test with same vectors
   - Verify results match Zig golden vectors exactly

3. **Java SDK** (src/clients/java/src/main/java/com/archerdb/geo/ShardingStrategy.java):
   - Find existing jumpHash implementation
   - Add testJumpHashGoldenVectors test with same vectors
   - Verify results match Zig golden vectors exactly

4. **Node.js SDK** (src/clients/node/src/geo.ts):
   - Find existing jumpHash implementation
   - Add jumpHash golden vector tests with same vectors
   - Verify results match Zig golden vectors exactly

If any SDK's jump hash implementation is missing or produces different results, implement/fix it to match the Zig canonical implementation exactly.

Document the golden vectors in a comment at the top of each SDK's sharding code referencing src/sharding.zig as the source of truth.
  </action>
  <verify>cd /home/g/archerdb/src/clients/go && go test -v -run TestJumpHash 2>/dev/null || echo "Go tests need verification"; cd /home/g/archerdb && ./zig/zig build test:unit -- --test-filter "sharding"</verify>
  <done>All 4 SDKs have golden vector tests that pass with values matching Zig implementation exactly</done>
</task>

</tasks>

<verification>
Run all sharding-related tests:
```bash
# Zig sharding tests
./zig/zig build test:unit -- --test-filter "sharding"
./zig/zig build test:unit -- --test-filter "jumpHash"

# Verify build passes
./zig/zig build
```
</verification>

<success_criteria>
- [ ] Golden vector tests exist and pass in Zig
- [ ] Distribution within 5% for all shard counts 8-256
- [ ] Resharding moves ~1/(N+1) entities (verified)
- [ ] All 4 SDKs have matching golden vector tests
- [ ] No modifications to core jumpHash algorithm
- [ ] All tests pass: `./zig/zig build test:unit -- --test-filter "sharding"`
</success_criteria>

<output>
After completion, create `.planning/phases/05-sharding-cleanup/05-01-SUMMARY.md`
</output>
