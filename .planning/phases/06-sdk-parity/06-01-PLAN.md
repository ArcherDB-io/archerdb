---
phase: 06-sdk-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clients/c/arch_client.h
  - src/clients/c/arch_client_header.zig
  - src/clients/c/README.md
  - src/clients/c/samples/main.c
autonomous: true

must_haves:
  truths:
    - "C header file has Doxygen comments for all public types and functions"
    - "C SDK has a README with quick start guide"
    - "C sample code demonstrates all geospatial operations"
    - "Memory ownership rules are documented"
    - "Thread safety is documented"
  artifacts:
    - path: "src/clients/c/arch_client.h"
      provides: "Auto-generated header with Doxygen documentation"
      contains: "@brief"
    - path: "src/clients/c/README.md"
      provides: "Quick start guide for C SDK"
      min_lines: 100
    - path: "src/clients/c/samples/main.c"
      provides: "Sample code for all operations"
      contains: ["query_radius", "query_polygon", "delete_entities"]
  key_links:
    - from: "src/clients/c/arch_client_header.zig"
      to: "src/clients/c/arch_client.h"
      via: "Header generation"
      pattern: "arch_client\\.h"
---

<objective>
Complete C SDK documentation and samples (SDKC-01 through SDKC-07)

Purpose: C SDK users need comprehensive documentation to use ArcherDB correctly, including memory management patterns and thread safety guidelines that are critical in C.

Output: Documented arch_client.h with Doxygen comments, README with quick start, complete sample code
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-sdk-parity/06-CONTEXT.md
@.planning/phases/06-sdk-parity/06-RESEARCH.md
@src/clients/c/arch_client.h
@src/clients/c/arch_client_header.zig
@src/clients/c/samples/main.c
@docs/error-codes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Doxygen documentation to header generator</name>
  <files>src/clients/c/arch_client_header.zig, src/clients/c/arch_client.h</files>
  <action>
Update arch_client_header.zig to generate Doxygen-style documentation comments in arch_client.h:

1. Add file-level documentation at the top of arch_client.h:
   - @file arch_client.h
   - @brief ArcherDB C Client - High-performance geospatial database client
   - @version matching current version
   - Memory ownership section explaining caller vs library ownership
   - Thread safety section explaining client is NOT thread-safe (one client per thread)

2. For each struct type, add:
   - @brief one-line description
   - @details multi-line explanation where needed
   - Field documentation with @param-style comments

3. For each enum, add:
   - @brief description
   - @details usage guidance
   - Per-value documentation

4. For each function-like macro or function pointer typedef:
   - @brief description
   - @param for each parameter
   - @return for return value
   - @note for important caveats (memory ownership, thread safety)

5. Document error code ranges inline with error_codes.md:
   - 0: Success
   - 1-99: Protocol errors
   - 100-199: Validation errors
   - 200-299: State errors (multi-region 213-218, sharding 220-224)
   - 300-399: Resource errors
   - 400-499: Security errors (encryption 410-414)
   - 500-599: Internal errors

Regenerate arch_client.h after updating the generator.

Key documentation patterns:
- geo_event_t: Document each field's unit (nano degrees, milliseconds, etc.)
- query filters: Document required vs optional fields
- INSERT_GEO_EVENT_RESULT: Document when each error occurs
  </action>
  <verify>
Run: `./zig/zig build` - must succeed
Verify arch_client.h contains Doxygen comments: `grep -c "@brief" src/clients/c/arch_client.h` should be > 20
Verify memory ownership documented: `grep -i "ownership" src/clients/c/arch_client.h`
  </verify>
  <done>
arch_client.h contains comprehensive Doxygen documentation for all public types, with memory ownership and thread safety documented
  </done>
</task>

<task type="auto">
  <name>Task 2: Create C SDK README with quick start</name>
  <files>src/clients/c/README.md</files>
  <action>
Create comprehensive README.md for C SDK:

1. Header:
   - Title: archerdb-c
   - Brief description: The ArcherDB C client for high-performance geospatial data storage

2. Prerequisites section:
   - Linux >= 5.6 (production), macOS/Windows for development
   - C compiler (gcc, clang)
   - Note: Header is auto-generated, do not modify directly

3. Installation section:
   - Copy arch_client.h and link against libarch_client_{arch}-{os}.a
   - Show linker flags for each platform (from arch_client_header.zig CGO flags)

4. Quick Start code example:
   - Initialize client with cluster_id and address
   - Create and insert a geo_event_t
   - Query by radius
   - Query by polygon (brief)
   - Query by UUID
   - Delete entities
   - Cleanup and deinit

5. API Reference section (brief):
   - arch_client_init / arch_client_deinit
   - arch_client_submit
   - Callback function signature

6. Memory Management section:
   - Client owns connection resources
   - Caller owns geo_event_t arrays passed to insert
   - Result callbacks receive library-owned data (do not free)
   - Document when to copy vs reference

7. Thread Safety section:
   - Client is NOT thread-safe
   - Create one client per thread, or use external synchronization
   - Callbacks may be invoked from different threads (document)

8. Error Handling section:
   - Check return codes from all functions
   - Link to error-codes.md for full reference
   - Show example error handling pattern

9. Sample Projects link

Format as proper markdown with code blocks for C examples.
  </action>
  <verify>
File exists and has content: `wc -l src/clients/c/README.md` should be > 100
Contains key sections: `grep -E "(Prerequisites|Quick Start|Memory Management|Thread Safety)" src/clients/c/README.md`
  </verify>
  <done>
README.md provides complete quick start guide with memory management and thread safety documentation
  </done>
</task>

<task type="auto">
  <name>Task 3: Complete C sample code with all operations</name>
  <files>src/clients/c/samples/main.c</files>
  <action>
Update main.c to demonstrate all geospatial operations:

1. Review existing code and identify what's already demonstrated

2. Ensure these operations are demonstrated (add if missing):
   - Insert events (batch)
   - Upsert events
   - Query by UUID (get_latest_by_uuid equivalent)
   - Query by radius with pagination handling
   - Query by polygon (define polygon vertices, query)
   - Query latest events
   - Delete entities
   - Error handling for each operation type

3. Add comprehensive comments explaining:
   - What each operation does
   - Parameter units (nanodegrees, millimeters, nanoseconds)
   - Expected results
   - Error handling approach

4. Demonstrate error code checking:
   - Check INSERT_GEO_EVENT_RESULT values
   - Handle connection errors
   - Handle validation errors

5. Show proper resource cleanup:
   - Free any allocated memory
   - Call arch_client_deinit

6. Add header comment with:
   - File description
   - Build instructions
   - Usage instructions

Ensure code compiles with `./zig/zig build` (C samples may not be in default build, check build.zig)
  </action>
  <verify>
File contains all operations: `grep -E "(query_radius|query_polygon|delete_entities|upsert)" src/clients/c/samples/main.c`
Code compiles: `./zig/zig build` succeeds
  </verify>
  <done>
main.c demonstrates all geospatial operations with comprehensive comments, error handling, and resource cleanup
  </done>
</task>

</tasks>

<verification>
All C SDK requirements (SDKC-01 through SDKC-07) addressed:
- SDKC-01: Geospatial operations available (via header types) - VERIFY IN HEADER
- SDKC-02: Error codes mapped (documented in header) - `grep "error" src/clients/c/arch_client.h`
- SDKC-03: Header documented (Doxygen) - `grep "@brief" src/clients/c/arch_client.h`
- SDKC-04: Memory management documented - Check README.md section
- SDKC-05: Thread safety documented - Check README.md section
- SDKC-06: Sample code for all operations - Check samples/main.c
- SDKC-07: Test coverage - C tests use Zig test framework, existing tests cover header generation
</verification>

<success_criteria>
1. arch_client.h has Doxygen comments with @brief for all public types
2. Memory ownership rules documented in header and README
3. Thread safety documented in README
4. README has > 100 lines with quick start guide
5. samples/main.c demonstrates insert, query (radius, polygon, UUID), and delete
6. `./zig/zig build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-sdk-parity/06-01-SUMMARY.md`
</output>
