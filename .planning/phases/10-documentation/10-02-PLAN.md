---
phase: 10-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/openapi.yaml
  - docs/api-reference.md
autonomous: true

must_haves:
  truths:
    - "All API operations are documented with request/response examples"
    - "OpenAPI spec is valid and matches markdown documentation"
    - "Error codes have example requests that trigger them"
  artifacts:
    - path: "docs/openapi.yaml"
      provides: "Machine-readable API specification"
      contains: "openapi: 3.0"
    - path: "docs/api-reference.md"
      provides: "Human-readable API documentation"
      contains: "## Operations"
  key_links:
    - from: "docs/api-reference.md"
      to: "docs/openapi.yaml"
      via: "Reference to OpenAPI spec"
      pattern: "openapi.yaml"
---

<objective>
Complete API reference documentation with OpenAPI spec and comprehensive examples.

Purpose: DOCS-02 requires complete API reference for all operations. The existing api-reference.md (1245 lines) is comprehensive but lacks machine-readable OpenAPI spec. This plan adds OpenAPI 3.0 spec and ensures all operations have curl + SDK examples.

Output: New docs/openapi.yaml and enhanced docs/api-reference.md with complete operation coverage.
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-documentation/10-CONTEXT.md
@.planning/phases/10-documentation/10-RESEARCH.md
@docs/api-reference.md
@docs/error-codes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenAPI 3.0 specification</name>
  <files>docs/openapi.yaml</files>
  <action>
Create OpenAPI 3.0 spec matching the existing api-reference.md documentation:

1. Use OpenAPI 3.0.3 format

2. Document all operations from api-reference.md:
   - createBatch / commit (insert events)
   - queryRadius (spatial query by radius)
   - queryPolygon (spatial query by polygon)
   - queryEntity (get entity history)
   - queryRange (get events in time range)
   - deleteEvents (remove events)
   - cleanupExpired (TTL cleanup)
   - getStats (cluster statistics)

3. For each operation, include:
   - Summary and description
   - Request body schema with all fields from GeoEvent type
   - Response schema
   - Example request/response
   - Error responses (400, 404, 500)

4. Define reusable schemas in components:
   - GeoEvent (with all 12 fields from api-reference.md)
   - EventResult
   - QueryResult
   - ErrorResponse

5. Use San Francisco coordinates in all examples (37.7749, -122.4194)

6. Include server configuration:
   - Default: http://localhost:3000
   - Production example: https://archerdb.example.com

7. Add tags for operation grouping:
   - Write Operations
   - Query Operations
   - Administrative Operations
  </action>
  <verify>
Validate OpenAPI spec:
- Online validator: https://editor.swagger.io (paste content)
- Or: `npm install -g @apidevtools/swagger-cli && swagger-cli validate docs/openapi.yaml`
- All operations from api-reference.md are present
- All GeoEvent fields documented
  </verify>
  <done>Valid OpenAPI 3.0 spec documents all ArcherDB operations</done>
</task>

<task type="auto">
  <name>Task 2: Enhance api-reference.md with complete examples</name>
  <files>docs/api-reference.md</files>
  <action>
Enhance api-reference.md to ensure DOCS-02 compliance:

1. Add reference to OpenAPI spec at top:
   ```markdown
   > Machine-readable API spec available at [openapi.yaml](openapi.yaml)
   ```

2. Verify all operations have:
   - Request body table with Field, Type, Required, Description
   - Response body table
   - curl example (copy-paste ready)
   - Error response examples

3. Enhance error documentation section:
   - For each error code in error-codes.md, add:
     - Example request that triggers the error
     - Corrected request showing how to fix
   - Key errors to document:
     - InvalidLatitude (lat_nano out of range)
     - InvalidLongitude (lon_nano out of range)
     - BatchTooLarge (>10,000 events)
     - EntityNotFound (query for non-existent entity)

4. Add "Common Patterns" section:
   - Pagination (using cursor/limit)
   - Idempotent upsert pattern
   - Batch insert optimization (optimal batch size)
   - Error retry pattern (which errors are retryable)

5. Ensure all examples use San Francisco coordinates consistently

6. Cross-link to SDK documentation:
   - "For language-specific examples, see: Python | Node.js | Go | Java"
  </action>
  <verify>
- All 8 operations have complete documentation
- All operations have curl examples
- Error section shows example triggering requests
- Links to openapi.yaml present
- Links to SDK READMEs present
  </verify>
  <done>API reference is complete with examples for all operations and error cases</done>
</task>

</tasks>

<verification>
DOCS-02 requirement verification:
1. Count operations in openapi.yaml matches api-reference.md
2. All GeoEvent fields documented in both
3. Error examples are accurate (can be tested against running server)
</verification>

<success_criteria>
- DOCS-02: API reference documents all operations with examples
- OpenAPI spec is valid and matches documentation
- Error codes have example requests showing what triggers them
- SDK documentation links present
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-02-SUMMARY.md`
</output>
