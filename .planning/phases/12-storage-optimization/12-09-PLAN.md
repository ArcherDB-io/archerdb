---
phase: 12-storage-optimization
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/geo_state_machine.zig
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Adaptive compaction auto-tunes based on workload patterns without manual intervention"
  artifacts:
    - path: "src/geo_state_machine.zig"
      provides: "Wired adaptive_record_write/read/scan calls"
      contains: "forest.adaptive_record_write"
  key_links:
    - from: "src/geo_state_machine.zig"
      to: "forest.adaptive_record_write/read/scan"
      via: "Direct function calls in operation handlers"
      pattern: "adaptive_record_write|adaptive_record_read|adaptive_record_scan"
---

<objective>
Wire state machine operations to adaptive compaction tracking

Purpose: Close verification gap - adaptive module exists but is not receiving workload data.
The AdaptiveState has adaptive_record_write/read/scan functions ready but the state machine
never calls them, so adaptive compaction cannot detect workload patterns.

Output: State machine calls adaptive_record_* on each operation, enabling workload detection.
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-storage-optimization/12-VERIFICATION.md
@.planning/phases/12-storage-optimization/12-06-SUMMARY.md
@src/lsm/forest.zig (adaptive_record_write/read/scan methods at lines 1051-1065)
@src/geo_state_machine.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire write operations to adaptive tracking</name>
  <files>src/geo_state_machine.zig</files>
  <action>
    Add forest.adaptive_record_write() calls in the state machine's write operation handlers:

    1. In execute_insert_events() - after successfully inserting events into forest:
       - After `self.forest.grooves.geo_events.insert(&stored_event)` calls
       - Call `self.forest.adaptive_record_write(1)` for each successful insert
       - OR batch: track inserted_count and call `self.forest.adaptive_record_write(inserted_count)` at end

    2. In execute_upsert_events() - same pattern (uses execute_insert_events internally)
       - No changes needed if inserts are tracked in execute_insert_events

    3. In execute_delete_entities() - track deletes as writes:
       - After tombstone insertion: `self.forest.grooves.geo_events.insert(&tombstone)`
       - Call `self.forest.adaptive_record_write(1)` for each tombstone

    Best approach: Add a single call at the end of execute_insert_events after the loop,
    using inserted_count which is already tracked: `self.forest.adaptive_record_write(inserted_count)`

    Similarly for delete, after the tombstone insertion loop completes.
  </action>
  <verify>
    Build compiles: `./zig/zig build -j4 -Dconfig=lite`
    Grep shows write calls: `grep -n "adaptive_record_write" src/geo_state_machine.zig`
  </verify>
  <done>
    State machine calls adaptive_record_write() on insert/upsert/delete operations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire read operations to adaptive tracking</name>
  <files>src/geo_state_machine.zig</files>
  <action>
    Add forest.adaptive_record_read() calls in point query handlers:

    1. In execute_query_uuid() - point lookup by entity UUID:
       - After forest.grooves.geo_events.get() calls
       - Track successful lookups and call `self.forest.adaptive_record_read(count)`

    2. In execute_query_latest() - latest position lookup:
       - This uses RAM index primarily, but LSM fallback occurs
       - When LSM lookup happens via forest.grooves.geo_events.get()
       - Call `self.forest.adaptive_record_read(1)`

    Locate these functions by searching for `grooves.geo_events.get` and add
    adaptive_record_read() nearby for each point lookup.
  </action>
  <verify>
    Build compiles: `./zig/zig build -j4 -Dconfig=lite`
    Grep shows read calls: `grep -n "adaptive_record_read" src/geo_state_machine.zig`
  </verify>
  <done>
    State machine calls adaptive_record_read() on point query operations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire scan operations to adaptive tracking</name>
  <files>src/geo_state_machine.zig</files>
  <action>
    Add forest.adaptive_record_scan() calls in range query handlers:

    1. In execute_query_radius() - radius queries are range scans over S2 cells:
       - Track cells queried or results iterated
       - Call `self.forest.adaptive_record_scan(1)` per scan operation

    2. In execute_query_polygon() - polygon queries scan multiple regions:
       - Call `self.forest.adaptive_record_scan(1)` per scan operation

    3. In execute_query_bbox() - bounding box is also a range scan:
       - Call `self.forest.adaptive_record_scan(1)` per scan operation

    Note: Range scans may not directly call forest.grooves.geo_events - they may
    operate on RAM index. Track scan operations where LSM range iteration occurs.

    If scans primarily use RAM index without LSM, add the call anyway to track
    scan-heavy workloads (the metric is about workload patterns, not just LSM I/O).
  </action>
  <verify>
    Build compiles: `./zig/zig build -j4 -Dconfig=lite`
    Grep shows scan calls: `grep -n "adaptive_record_scan" src/geo_state_machine.zig`
  </verify>
  <done>
    State machine calls adaptive_record_scan() on range query operations.
  </done>
</task>

</tasks>

<verification>
After all tasks:

1. Build succeeds:
   ```
   ./zig/zig build -j4 -Dconfig=lite
   ```

2. All three adaptive_record_* functions are called from state machine:
   ```
   grep -c "adaptive_record_" src/geo_state_machine.zig
   # Should show 3 or more matches
   ```

3. Unit tests pass:
   ```
   ./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "geo_state"
   ```
</verification>

<success_criteria>
- State machine calls forest.adaptive_record_write() on insert/delete operations
- State machine calls forest.adaptive_record_read() on point query operations
- State machine calls forest.adaptive_record_scan() on range query operations
- Build compiles without errors
- Gap "Adaptive compaction auto-tunes based on workload patterns" is now closable
  (the module receives workload data and can detect patterns)
</success_criteria>

<output>
After completion, create `.planning/phases/12-storage-optimization/12-09-SUMMARY.md`
</output>
