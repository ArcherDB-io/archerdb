---
phase: 08-operations-tooling
plan: 06
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05"]
files_modified:
  - .planning/phases/08-operations-tooling/08-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "All 10 OPS requirements have documented verification status"
    - "Helm chart validated via lint and dry-run"
    - "Backup and DR documentation complete"
    - "Upgrade procedures documented and CLI available"
  artifacts:
    - path: ".planning/phases/08-operations-tooling/08-VERIFICATION.md"
      provides: "Phase verification report"
      contains: "PASS"
  key_links:
    - from: ".planning/phases/08-operations-tooling/08-VERIFICATION.md"
      to: ".planning/ROADMAP.md"
      via: "phase completion status"
      pattern: "Phase 8"
---

<objective>
Verify all Phase 8 requirements are met and document verification results.

Purpose: Confirm Phase 8 (Operations Tooling) is complete with all OPS requirements addressed.

Output: Phase verification report documenting status of all requirements.
</objective>

<execution_context>
@/home/g/.claude/get-shit-done/workflows/execute-plan.md
@/home/g/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-operations-tooling/08-01-SUMMARY.md
@.planning/phases/08-operations-tooling/08-02-SUMMARY.md
@.planning/phases/08-operations-tooling/08-03-SUMMARY.md
@.planning/phases/08-operations-tooling/08-04-SUMMARY.md
@.planning/phases/08-operations-tooling/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run verification checks</name>
  <files>
    (verification commands only - no file modifications)
  </files>
  <action>
Execute verification commands for all deliverables:

1. **Helm chart validation**:
   ```bash
   helm lint deploy/helm/archerdb
   helm template archerdb deploy/helm/archerdb | kubectl apply --dry-run=client -f -
   helm template archerdb deploy/helm/archerdb -f deploy/helm/archerdb/values-production.yaml | kubectl apply --dry-run=client -f -
   ```

2. **Build verification**:
   ```bash
   ./zig/zig build -j4 -Dconfig=lite check
   ```

3. **Documentation existence**:
   ```bash
   ls -la docs/backup-operations.md docs/upgrade-guide.md docs/disaster-recovery.md
   ```

4. **Script syntax validation**:
   ```bash
   bash -n scripts/dr-test.sh
   ```

5. **Template rendering verification**:
   ```bash
   helm template archerdb deploy/helm/archerdb | grep "kind: StatefulSet"
   helm template archerdb deploy/helm/archerdb | grep "kind: PodDisruptionBudget"
   helm template archerdb deploy/helm/archerdb --set metrics.serviceMonitor.enabled=true | grep "kind: ServiceMonitor"
   helm template archerdb deploy/helm/archerdb --set metrics.alerts.enabled=true | grep "kind: PrometheusRule"
   ```
  </action>
  <verify>
    All verification commands succeed
  </verify>
  <done>
    All verification commands pass. Results captured for report.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create verification report</name>
  <files>
    .planning/phases/08-operations-tooling/08-VERIFICATION.md
  </files>
  <action>
Create comprehensive verification report:

```markdown
# Phase 8: Operations Tooling - Verification Report

**Verified:** [DATE]
**Status:** COMPLETE

## Requirements Verification

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| OPS-01 | K8s manifests deploy 3-node cluster | PASS | Helm lint + dry-run succeed |
| OPS-02 | Health probes enable zero-downtime rolling updates | PASS | PDB + probes in StatefulSet |
| OPS-03 | HPA based on load | PARTIAL | KEDA template prepared, not deployed |
| OPS-04 | Online backup without downtime | PASS | follower_only mode in backup coordinator |
| OPS-05 | Incremental backup | PASS | Sequence tracking in backup state |
| OPS-06 | DR plan documented and tested | PASS | disaster-recovery.md + dr-test.sh |
| OPS-07 | Upgrade procedure tested | PASS | upgrade.zig + upgrade-guide.md |
| OPS-08 | Rollback procedure tested | PASS | Rollback in upgrade.zig + docs |
| OPS-09 | Capacity planning guidelines | PASS | Existing docs/capacity-planning.md |
| OPS-10 | Runbooks for failure scenarios | PASS | Existing docs/operations-runbook.md |

## Verification Commands Run

### Helm Chart
- `helm lint deploy/helm/archerdb` - PASS
- `helm template | kubectl apply --dry-run=client` - PASS
- Production values render - PASS

### Build
- `./zig/zig build -j4 -Dconfig=lite check` - PASS

### Documentation
- backup-operations.md - EXISTS
- upgrade-guide.md - EXISTS
- disaster-recovery.md - ENHANCED

### Scripts
- dr-test.sh syntax - VALID

## Deliverables Summary

### Helm Chart (deploy/helm/archerdb/)
- Chart.yaml, values.yaml, values-production.yaml
- templates/: statefulset, service, configmap, pdb, servicemonitor, prometheusrule
- README.md with usage instructions

### Code
- src/archerdb/upgrade.zig - Upgrade orchestration
- src/archerdb/backup_coordinator.zig - follower_only mode

### Documentation
- docs/backup-operations.md - Complete backup guide
- docs/upgrade-guide.md - Rolling upgrade procedures
- docs/disaster-recovery.md - Enhanced with RTO/RPO targets

### Scripts
- scripts/dr-test.sh - DR test automation

## Notes

- OPS-03 (HPA): KEDA ScaledObject template prepared but marked as opt-in.
  KEDA only makes sense for read replicas (write quorum is fixed).
  Full KEDA deployment requires KEDA operator installed in cluster.

- OPS-09/OPS-10: Leveraged existing documentation from previous work.
  docs/capacity-planning.md and docs/operations-runbook.md were already complete.

## Success Criteria

1. [x] Kubernetes manifests deploy working 3-node cluster (via Helm)
2. [x] Rolling updates complete without downtime (PDB + probes)
3. [x] Online backup runs without impacting client traffic (follower-only)
4. [x] Disaster recovery plan documented and tested (dr-test.sh)
5. [x] Upgrade from version N to N+1 tested and documented (upgrade CLI + docs)

**Phase 8: COMPLETE**
```

Update the report with actual verification results from Task 1.
  </action>
  <verify>
    test -f /home/g/archerdb/.planning/phases/08-operations-tooling/08-VERIFICATION.md && grep "COMPLETE" /home/g/archerdb/.planning/phases/08-operations-tooling/08-VERIFICATION.md
  </verify>
  <done>
    08-VERIFICATION.md exists with COMPLETE status. All OPS requirements documented with verification evidence.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update project state</name>
  <files>
    .planning/STATE.md
  </files>
  <action>
Update STATE.md to reflect Phase 8 completion:

1. Update "Current Position" section:
   - Phase: 8 of 10 (Operations Tooling) - COMPLETE
   - Plan: 6 of 6 in current phase (complete)
   - Status: Phase 8 complete
   - Last activity: [DATE] - Completed 08-06-PLAN.md (Phase Verification)

2. Add Phase 8 Completion Status section (after Phase 7):
   ```markdown
   ## Phase 8 Completion Status

   **VERIFIED COMPLETE** - All 10 OPS requirements addressed:

   | Requirement | Description | Status |
   |-------------|-------------|--------|
   | OPS-01 | K8s manifests deploy 3-node cluster | PASS |
   | OPS-02 | Health probes enable zero-downtime updates | PASS |
   | OPS-03 | HPA based on load | PARTIAL (opt-in KEDA) |
   | OPS-04 | Online backup without downtime | PASS |
   | OPS-05 | Incremental backup | PASS |
   | OPS-06 | DR plan documented and tested | PASS |
   | OPS-07 | Upgrade procedure tested | PASS |
   | OPS-08 | Rollback procedure tested | PASS |
   | OPS-09 | Capacity planning guidelines | PASS |
   | OPS-10 | Runbooks for failure scenarios | PASS |

   **Key Deliverables:**
   - Helm chart in deploy/helm/archerdb/
   - Upgrade CLI with health-based rollback
   - Follower-only online backup
   - DR test automation script

   **Verification Report:** `.planning/phases/08-operations-tooling/08-VERIFICATION.md`
   ```

3. Update progress bar: 96% (29/30 plans) - or whatever the actual count is

4. Update "Next" line: Phase 9 (Testing Infrastructure)
  </action>
  <verify>
    grep "Phase 8" /home/g/archerdb/.planning/STATE.md | head -3
  </verify>
  <done>
    STATE.md updated with Phase 8 completion status. Progress bar updated. Next phase identified.
  </done>
</task>

</tasks>

<verification>
1. All verification commands from Task 1 succeed
2. 08-VERIFICATION.md documents all 10 OPS requirements
3. STATE.md updated with Phase 8 completion
4. Phase 8 success criteria met:
   - K8s manifests via Helm chart
   - Rolling updates with PDB
   - Online backup (follower-only)
   - DR procedures tested
   - Upgrade/rollback documented
</verification>

<success_criteria>
- All 10 OPS requirements have documented status
- Verification report created with evidence
- STATE.md updated reflecting phase completion
- Phase 8 marked COMPLETE in project tracking
</success_criteria>

<output>
After completion, create `.planning/phases/08-operations-tooling/08-06-SUMMARY.md`
</output>
