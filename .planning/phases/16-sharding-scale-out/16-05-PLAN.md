---
phase: 16-sharding-scale-out
plan: 05
type: execute
wave: 1
depends_on: ["16-02"]
files_modified:
  - src/archerdb/cli.zig
  - src/archerdb/main.zig
  - src/sharding.zig
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "Operator can trigger online resharding via shard CLI and see migration start."
    - "Online resharding ticks migration batches and updates dual-write/progress metrics during runtime."
    - "Cutover or rollback transitions update topology notifications and reset resharding metrics."
  artifacts:
    - path: "src/archerdb/main.zig"
      provides: "Runtime entrypoint to start/cancel online resharding"
    - path: "src/archerdb/cli.zig"
      provides: "CLI wiring for shard reshard --mode=online"
    - path: "src/sharding.zig"
      provides: "Online resharding runner/helper driving controller ticks"
  key_links:
    - from: "src/archerdb/main.zig"
      to: "sharding.OnlineReshardingController.startOnlineResharding"
      via: "command_shard online reshard handler"
      pattern: "startOnlineResharding"
    - from: "src/archerdb/main.zig"
      to: "sharding.OnlineReshardingController.tickMigration"
      via: "migration loop"
      pattern: "tickMigration"
    - from: "src/archerdb/main.zig"
      to: "TopologyManager.beginResharding|completeResharding|abortResharding"
      via: "controller lifecycle"
      pattern: "beginResharding|completeResharding|abortResharding"
---

<objective>
Wire online resharding into runtime so operators can trigger a live migration and the controller drives metrics/topology updates.

Purpose: Close the verification gap where OnlineReshardingController exists but is only exercised in tests.
Output: Runtime/CLI entrypoint that drives controller start, tick, cutover, and rollback flows.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-sharding-scale-out/16-sharding-scale-out-VERIFICATION.md
@.planning/phases/16-sharding-scale-out/16-02-SUMMARY.md

@src/archerdb/cli.zig
@src/archerdb/main.zig
@src/sharding.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement online resharding runtime entrypoint</name>
  <files>src/archerdb/main.zig, src/sharding.zig</files>
  <action>
    - Add a helper in main.zig (e.g., run_online_resharding) that instantiates
      sharding.OnlineReshardingController with OnlineReshardingConfig{} and optional TopologyManager.
    - Drive the controller lifecycle: startOnlineResharding(target_shards, total_entities),
      then loop through MigrationBatch instances, calling tickMigration and maybeCutover until complete.
    - Use OnlineReshardingConfig.batch_size to size batches; generate deterministic entity_ids
      (sequential u128 or sampled IDs) so progress and ETA metrics advance while still allowing rollback.
    - On error or operator cancel, call controller.cancel(reason) and ensure metrics reset.
    - Log state transitions (preparing → migrating → finalizing) so CLI output confirms runtime wiring.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "OnlineReshardingController"</verify>
  <done>Online resharding can be started and progresses via controller ticks with metrics updating.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shard CLI to trigger online resharding</name>
  <files>src/archerdb/cli.zig, src/archerdb/main.zig</files>
  <action>
    - Extend the shard reshard command handling to route mode=online to the new runtime helper.
    - Keep offline mode behavior unchanged; only online mode invokes OnlineReshardingController.
    - Honor --dry-run by printing intended action without invoking the controller.
    - Provide clear stdout/stderr messaging that online resharding has started and which shard count
      is targeted so operators can confirm activation.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite check</verify>
  <done>archerdb shard reshard --mode=online invokes the live controller path (non-dry-run).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Runtime online resharding command that drives controller ticks</what-built>
  <how-to-verify>
    1) Start ArcherDB with metrics enabled.
    2) Run: archerdb shard reshard --mode=online --to=<new_shard_count> <path>.
    3) Confirm /health/shards shows resharding_progress moving and dual_write enabled.
    4) Ensure requests continue without downtime during migration and cutover.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Unit tests for OnlineReshardingController still pass.
- CLI path compiles and logs online resharding activation.
</verification>

<success_criteria>
- Online resharding is triggered via CLI in runtime (not just tests).
- Controller tick/cutover/rollback paths execute and update metrics/topology status.
- Operators can observe progress in /health/shards without downtime.
</success_criteria>

<output>
After completion, create `.planning/phases/16-sharding-scale-out/16-05-SUMMARY.md`
</output>
