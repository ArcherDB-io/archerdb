---
phase: 16-sharding-scale-out
plan: 04
type: execute
wave: 2
depends_on: ["03"]
files_modified:
  - src/archerdb/observability/trace_export.zig
  - src/archerdb/observability.zig
  - src/coordinator.zig
autonomous: true

must_haves:
  truths:
    - "Distributed traces include per-shard spans linked to the root request."
    - "Span attributes capture shard ids and result counts for fan-out queries."
    - "OTLP exporter emits span links and attributes in JSON output."
  artifacts:
    - path: "src/archerdb/observability/trace_export.zig"
      provides: "Span links + attribute serialization for OTLP export"
      contains: "SpanLink"
    - path: "src/coordinator.zig"
      provides: "Coordinator emits OTel spans for fan-out queries"
      contains: "trace_exporter"
  key_links:
    - from: "src/coordinator.zig"
      to: "archerdb/observability/trace_export.zig"
      via: "OtlpTraceExporter.recordSpan"
      pattern: "recordSpan"
---

<objective>
Add OpenTelemetry span links and integrate cross-shard tracing into coordinator fan-out.

Purpose: Provide full request-path visibility across shards using OTel spans and links.
Output: OTLP exporter supports links/attributes and coordinator records fan-out spans.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-sharding-scale-out/16-CONTEXT.md
@.planning/phases/16-sharding-scale-out/16-RESEARCH.md

@src/archerdb/observability/trace_export.zig
@src/archerdb/observability.zig
@src/coordinator.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add span link + attribute serialization to OTLP exporter</name>
  <files>src/archerdb/observability/trace_export.zig, src/archerdb/observability.zig</files>
  <action>
    Extend tracing structures to support span links and attributes:
    - Add SpanLink { trace_id, span_id, attributes }
    - Add links: []const SpanLink to Span
    - Update OwnedSpan to deep-copy attributes + links (copy keys/values and IDs)

    Update formatSpan to serialize:
    - attributes array (key + typed value) when present
    - links array with traceId/spanId and optional attributes

    Add helper for building spans with attributes/links (e.g., spanWithAttributes)
    and re-export SpanLink/Attribute in observability.zig. Add a unit test that
    formats OTLP JSON with attributes + links and asserts the serialized keys appear.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "trace_export"</verify>
  <done>OTLP exporter emits links/attributes and tests validate JSON output.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument coordinator fan-out spans with links</name>
  <files>src/coordinator.zig</files>
  <action>
    Extend CoordinatorConfig with optional trace_exporter: ?*observability.OtlpTraceExporter
    and store it on Coordinator. Update fanOutQuery to accept an optional
    *CorrelationContext (use newRoot when absent).

    When tracing is enabled:
    - Create a root span (SpanKind.server) with attributes:
      query_type, shards_queried, shards_succeeded, shards_failed
    - For each shard query, create a child span (SpanKind.client) with parent_span_id
      set to the root span id and add a SpanLink pointing to the root span.
      Include shard_id, shard_status, result_count, and error flag attributes.
    - Record spans via trace_exporter.recordSpan() after completion.

    Ensure spans are only recorded when the exporter is configured and the context
    is sampled (CorrelationContext.isSampled()).
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "Coordinator"</verify>
  <done>Coordinator emits root + per-shard spans with links and attributes.</done>
</task>

</tasks>

<verification>
- `./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "trace_export"`
- `./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "Coordinator"`
</verification>

<success_criteria>
- OTLP JSON includes span links and attribute payloads for fan-out spans.
- Coordinator records root + shard spans with link-based fan-out visibility.
- Tracing is gated by configured exporter and sampling flag.
</success_criteria>

<output>
After completion, create `.planning/phases/16-sharding-scale-out/16-04-SUMMARY.md`
</output>
