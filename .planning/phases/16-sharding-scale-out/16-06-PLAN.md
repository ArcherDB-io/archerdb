---
phase: 16-sharding-scale-out
plan: 06
type: execute
wave: 2
depends_on: ["16-03", "16-04", "16-05"]
files_modified:
  - src/archerdb/cli.zig
  - src/archerdb/main.zig
  - src/coordinator.zig
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "Cross-shard queries in the coordinator execute via fanOutQuery and return shard counts."
    - "Partial fan-out failures are surfaced in coordinator results/metrics for live requests."
    - "Distributed traces emit root + per-shard spans with links when OTLP export is enabled."
  artifacts:
    - path: "src/coordinator.zig"
      provides: "Live query path that invokes fanOutQuery and returns shard counts"
    - path: "src/archerdb/main.zig"
      provides: "Coordinator startup wiring with OTLP exporter"
    - path: "src/archerdb/cli.zig"
      provides: "Coordinator CLI flags for trace export configuration"
  key_links:
    - from: "src/coordinator.zig"
      to: "Coordinator.fanOutQuery"
      via: "live query handling"
      pattern: "fanOutQuery"
    - from: "src/archerdb/main.zig"
      to: "observability.OtlpTraceExporter.init"
      via: "coordinator startup"
      pattern: "OtlpTraceExporter.init"
    - from: "src/coordinator.zig"
      to: "observability.CorrelationContext"
      via: "trace context propagation"
      pattern: "CorrelationContext"
---

<objective>
Activate live fan-out query execution and tracing by wiring coordinator query handling to fanOutQuery and enabling OTLP export.

Purpose: Close gaps where fanOutQuery/tracing exist only in tests by ensuring runtime paths invoke them.
Output: Coordinator query handling uses fanOutQuery, surfaces shard counts, and emits OTLP spans with links.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-sharding-scale-out/16-sharding-scale-out-VERIFICATION.md
@.planning/phases/16-sharding-scale-out/16-03-SUMMARY.md
@.planning/phases/16-sharding-scale-out/16-04-SUMMARY.md

@src/coordinator.zig
@src/archerdb/cli.zig
@src/archerdb/main.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire coordinator query handling to fanOutQuery</name>
  <files>src/coordinator.zig, src/archerdb/main.zig</files>
  <action>
    - Add a live query handler in coordinator.zig (e.g., executeQuery) that:
      - Uses routeQuery for single-shard uuid lookups.
      - Uses fanOutQuery for uuid_batch/radius/polygon/latest with defaultFanOutPolicy.
      - Returns shard counts/partial flag (FanOutResult) for response/logging.
    - Update the coordinator start path in main.zig to invoke this handler when processing
      incoming coordinator requests (even if wired through a minimal stub or adapter for now).
    - Ensure shard success/failure counts are surfaced in the handler output so callers can
      observe partial failures for live queries.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "Coordinator: fan-out"</verify>
  <done>fanOutQuery is called from a non-test runtime path and returns shard counts to the caller.</done>
</task>

<task type="auto">
  <name>Task 2: Enable OTLP exporter + correlation context for fan-out traces</name>
  <files>src/archerdb/cli.zig, src/archerdb/main.zig, src/coordinator.zig</files>
  <action>
    - Extend coordinator CLI args to accept trace-export/otlp-endpoint flags (mirror start command).
    - In command_coordinator start, initialize observability.OtlpTraceExporter when enabled and
      pass it into CoordinatorConfig.trace_exporter.
    - Parse incoming trace context (traceparent/B3) into CorrelationContext and pass it to
      Coordinator.fanOutQuery so spans are linked to the root request span.
    - Ensure the exporter is flushed/closed on shutdown so spans are emitted.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite check</verify>
  <done>Coordinator fan-out spans emit with links/attributes when OTLP export is enabled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Coordinator fan-out execution with OTLP trace export</what-built>
  <how-to-verify>
    1) Run coordinator with --trace-export=otlp --otlp-endpoint=<collector>.
    2) Execute a fan-out query (radius/polygon/latest) through the coordinator.
    3) Confirm collector shows root span with linked per-shard spans and shard/result attributes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Coordinator fan-out unit tests still pass.
- Coordinator builds with OTLP exporter wiring enabled.
</verification>

<success_criteria>
- Live coordinator query path invokes fanOutQuery and returns shard success/failure counts.
- OTLP exporter can be enabled via CLI and emits linked fan-out spans.
- Distributed tracing covers the full request path across shards.
</success_criteria>

<output>
After completion, create `.planning/phases/16-sharding-scale-out/16-06-SUMMARY.md`
</output>
