---
phase: 16-sharding-scale-out
plan: 07
type: execute
wave: 2
depends_on: ["16-01", "16-05"]
files_modified:
  - src/archerdb/metrics_server.zig
  - src/archerdb/main.zig
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Hot shard detection automatically schedules a resharding request when thresholds are exceeded."
    - "Automatic resharding only triggers once per cooldown window and never when resharding is active."
    - "Operators can see auto-reshard decisions reflected in /health/shards and metrics without manual action."
  artifacts:
    - path: "src/archerdb/metrics_server.zig"
      provides: "Rebalance decision helper that emits auto-reshard recommendation"
    - path: "src/archerdb/main.zig"
      provides: "Runtime hook that consumes auto-reshard recommendation"
  key_links:
    - from: "src/archerdb/metrics_server.zig"
      to: "metrics.Registry.shard_rebalance_needed"
      via: "rebalance decision computation"
      pattern: "shard_rebalance_needed"
    - from: "src/archerdb/main.zig"
      to: "metrics_server.takeReshardingRequest"
      via: "auto-reshard scheduling"
      pattern: "takeReshardingRequest"
    - from: "src/archerdb/main.zig"
      to: "resharding_controller.startOnlineResharding"
      via: "auto-triggered reshard start"
      pattern: "startOnlineResharding"
---

<objective>
Close SHARD-05 by turning hot shard detection into an automatic reshard trigger with cooldown safeguards.

Purpose: Ensure hot shard detection does more than emit metrics by initiating resharding when imbalance persists.
Output: Runtime auto-reshard decision wired into the main loop and observable via metrics/health.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-sharding-scale-out/16-sharding-scale-out-VERIFICATION.md
@.planning/phases/16-sharding-scale-out/16-01-SUMMARY.md
@.planning/phases/16-sharding-scale-out/16-05-SUMMARY.md

@src/archerdb/metrics_server.zig
@src/archerdb/main.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract rebalance decision helper for auto-reshard</name>
  <files>src/archerdb/metrics_server.zig</files>
  <action>
    - Refactor the hot-shard/rebalance logic in /health/shards into a helper that returns:
      {rebalance_needed, cooldown_remaining_seconds, target_shards} and still updates metrics.Registry gauges.
    - Compute target_shards deterministically (current shard_count + 1) and only when
      hot_signal_active, cooldown_elapsed, and no resharding is active.
    - Ensure the helper does NOT mutate resharding_request_target directly; it should only
      provide the recommendation for the main loop.
    - Keep existing /health/shards JSON output unchanged apart from using the helper.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite test:unit -- --test-filter "MetricsServer: /health/shards includes rebalance fields"</verify>
  <done>Rebalance decision is reusable and still drives hot-shard metrics/health output.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-schedule resharding when rebalance is needed</name>
  <files>src/archerdb/main.zig</files>
  <action>
    - Call the new rebalance decision helper once per main loop tick.
    - When rebalance_needed=1 and no resharding is active, queue the reshard request by
      setting metrics_server’s resharding request target (reuse existing control flow used by /control/reshard).
    - Log an info message noting the auto-reshard trigger and target shard count.
    - Ensure the auto-trigger respects cooldown: only one request per cooldown window and
      no repeated triggers while resharding is active.
  </action>
  <verify>./zig/zig build -j4 -Dconfig=lite check</verify>
  <done>Hot shard detection automatically queues an online resharding request without manual action.</done>
</task>

</tasks>

<verification>
- Rebalance metrics/health output still renders hot shard fields.
- Auto-reshard request is enqueued only when thresholds are met and cooldown allows.
</verification>

<success_criteria>
- SHARD-05 satisfied: hot shard detection results in an automatic resharding trigger.
- Auto-trigger never fires while resharding is active and honors cooldown guards.
- /health/shards reflects rebalance state while auto-reshard is queued.
</success_criteria>

<output>
After completion, create `.planning/phases/16-sharding-scale-out/16-07-SUMMARY.md`
</output>
